# TozoAI-Chat-Api 重构总结报告

> 生成时间：2026-03-27 15:53:39（最后更新：2026-03-27 19:08）
> 涉及需求：14 项

---

## 变更记录（2026-03-27 第五次 — 重命名环境配置文件并同步所有引用）

### 需求
将 `conf/config.dev.yaml` 和 `conf/config.prod.yaml` 重命名为 `config_dev.yaml` 和 `config_prod.yaml`（点分隔→下划线分隔），与 Viper `SetConfigName()` 的命名规范保持一致，同时同步所有引用该文件名的地方。

### 改动文件清单

#### 1. `conf/config.dev.yaml` → `conf/config_dev.yaml`（物理重命名）

执行命令：
```cmd
ren "conf\config.dev.yaml" config_dev.yaml
```

文件内第一行注释同步更新：
```yaml
# 旧：# conf/config.dev.yaml
# 新：# conf/config_dev.yaml
```

#### 2. `conf/config.prod.yaml` → `conf/config_prod.yaml`（物理重命名）

执行命令：
```cmd
ren "conf\config.prod.yaml" config_prod.yaml
```

文件内第一行注释同步更新：
```yaml
# 旧：# conf/config.prod.yaml
# 新：# conf/config_prod.yaml
```

#### 3. `conf/loader.go`（4处改动）

**（1）包注释第 3 行** — 文件名格式描述：
```go
// 旧：// 2. 加载环境专属配置（config.dev.yaml / config.prod.yaml）
//         注意：文件名格式为 config.{env}.yaml（点分隔），使用 SetConfigFile() 直接指定路径...
// 新：// 2. 加载环境专属配置（config_dev.yaml / config_prod.yaml）
//         文件名格式为 config_{env}.yaml（下划线分隔），使用 SetConfigName() 直接搜索。
```

**（2）`Load()` 函数注释**：
```go
// 旧：// 2. 环境配置（config.{env}.yaml，如 config.dev.yaml / config.prod.yaml）
// 新：// 2. 环境配置（config_{env}.yaml，如 config_dev.yaml / config_prod.yaml）
```

**（3）第 4 步加载逻辑** — 从 `SetConfigFile()` 回归 `SetConfigName()`（文件名已改为下划线，Viper 可正常搜索）：
```go
// 旧：复杂的 os.Stat + filepath.Join + SetConfigFile() 双目录搜索
envConfigFile := fmt.Sprintf("config.%s.yaml", env)
for _, searchDir := range []string{"./conf", "."} {
    envConfigPath := filepath.Join(searchDir, envConfigFile)
    if _, statErr := os.Stat(envConfigPath); statErr == nil {
        v.SetConfigFile(envConfigPath)
        v.MergeInConfig()
        break
    }
}

// 新：简洁的 SetConfigName()（文件名与 Viper 规范一致，无需特殊处理）
envConfigName := fmt.Sprintf("config_%s", env)
v.SetConfigName(envConfigName)
v.SetConfigType("yaml")
v.MergeInConfig()
```

**（4）移除 `import "path/filepath"`**（不再使用 `filepath.Join`）：
```go
// 旧：
import (
    "fmt"
    "os"
    "path/filepath"   // ← 移除
    "reflect"
    "strings"
    ...
)

// 新：
import (
    "fmt"
    "os"
    "reflect"
    "strings"
    ...
)
```

#### 4. `conf/config.yaml`（注释第 3~4 行）
```yaml
# 旧：# 此文件定义了所有环境的默认配置项，环境特定配置可在 config.dev.yaml 或 config.prod.yaml 中覆盖。
#     # 注意：环境配置文件命名格式为 config.{env}.yaml（点分隔），加载器通过 SetConfigFile() 直接指定路径。
# 新：# 此文件定义了所有环境的默认配置项，环境特定配置可在 config_dev.yaml 或 config_prod.yaml 中覆盖。
```

### 验证结果

**编译：**
```
go build ./...  →  BUILD OK（零错误，filepath import 已移除）
```

**启动日志（logs/startup3.log）：**
```
已加载环境配置: config_dev.yaml   ✅（不再出现"未找到"提示）
已加载模型配置: azureai.yaml
已加载模型配置: openai.yaml
[TozoAI] 环境: dev | 服务地址: :8096 | 模型数: 2
INFO  Redis 初始化成功 {"addr": "127.0.0.1:6379"}
INFO  服务启动中... {"addr": ":8096", "env": "dev", "jwt_enabled": true, "rate_limit_enabled": false, "fallback_enabled": true}
```

**config_dev.yaml 覆盖效果：**

| 配置项 | config.yaml 默认 | config_dev.yaml 覆盖 | 实际值 | 状态 |
|--------|-----------------|---------------------|--------|------|
| `rate_limit.enabled` | `true` | `false` | `false` | ✅ |
| `fallback.enabled` | `false` | `true` | `true` | ✅ |
| `server.mode` | `debug` | `debug` | `debug` | ✅ |

---

## 变更记录（2026-03-27 第四次 — 修复 config.dev.yaml 未被加载）

### 问题根因

`loader.go` 第 50 行拼出的环境配置文件名为 `config_dev`（下划线），然后调用：

```go
v.SetConfigName("config_dev")   // Viper 搜索: ./conf/config_dev.yaml
v.MergeInConfig()               // 找不到文件 → 静默跳过，打印 "未找到环境配置"
```

而磁盘上的实际文件名是 `config.dev.yaml`（点分隔）。Viper 的 `SetConfigName()` 只接受不含扩展名和点的文件名，因此永远无法找到 `config.dev.yaml`。

### 修复方案

**改用 `SetConfigFile()` 直接指定完整路径**，不依赖 Viper 的文件名搜索逻辑。搜索顺序 `./conf/config.{env}.yaml` → `./config.{env}.yaml`，兼容不同工作目录启动。

### 改动文件清单

#### `conf/loader.go`

1. **新增 import `path/filepath`**（用于跨平台路径拼接）

2. **重写第 4 步（环境专属配置加载）**：

   旧代码：
   ```go
   envConfig := fmt.Sprintf("config_%s", env)   // → "config_dev"（下划线，与文件不符）
   v.SetConfigName(envConfig)
   if err := v.MergeInConfig(); err == nil { ... }
   ```

   新代码：
   ```go
   envConfigFile := fmt.Sprintf("config.%s.yaml", env)  // → "config.dev.yaml"（与磁盘文件一致）
   for _, searchDir := range []string{"./conf", "."} {
       envConfigPath := filepath.Join(searchDir, envConfigFile)
       if _, statErr := os.Stat(envConfigPath); statErr == nil {
           v.SetConfigFile(envConfigPath)   // 直接指定完整路径，无歧义
           if mergeErr := v.MergeInConfig(); mergeErr == nil {
               fmt.Printf("已加载环境配置: %s (%s)\n", envConfigFile, envConfigPath)
               envConfigLoaded = true
           }
           break
       }
   }
   ```

3. **更新包注释**：将 `config_{env}.yaml` 改为 `config.{env}.yaml`，并说明使用 `SetConfigFile()` 的原因

4. **更新 `Load()` 函数注释**：将 `config_{env}.yaml` 改为 `config.{env}.yaml`

#### `conf/config.yaml`

- 顶部注释新增说明：文件命名格式为 `config.{env}.yaml`，加载器通过 `SetConfigFile()` 直接指定路径

### 验证结果

**编译：**
```
go build ./...  →  BUILD OK（零错误）
```

**启动日志（logs/startup2.log）：**
```
已加载环境配置: config.dev.yaml (conf\config.dev.yaml)   ✅ 不再打印"未找到"
已加载模型配置: azureai.yaml
已加载模型配置: openai.yaml
[TozoAI] 环境: dev | 服务地址: :8096 | 模型数: 2
INFO  Redis 初始化成功
INFO  服务启动中... {"rate_limit_enabled": false, "fallback_enabled": true}
```

**config.dev.yaml 覆盖效果验证：**

| 配置项 | config.yaml 默认值 | config.dev.yaml 覆盖值 | 实际启动值 | 是否正确 |
|--------|---------------------|------------------------|------------|---------|
| `rate_limit.enabled` | `true` | `false` | `false` | ✅ |
| `fallback.enabled` | `false` | `true` | `true` | ✅ |
| `server.mode` | `debug` | `debug` | `debug` | ✅ |

---

## 变更记录（2026-03-27 第三次 — 全面审查 + 调试启动）

### 一、问题分析总览

| 文件 | 问题类型 | 严重程度 |
|------|---------|---------|
| `internal/service/redis/redis.go` | `GetClient()` Redis 未启用时 panic | 🔴 严重（崩溃） |
| `internal/service/session/manager.go` | `redis.Set()` 传入 `map[string]interface{}`，go-redis 不支持序列化 map | 🔴 严重（运行时错误） |
| `internal/service/billing/billing.go` | 5 处直接调用 `redis.GetClient()` 无 nil 检查 | 🔴 严重（nil panic） |
| `internal/middleware/rate.go` | 注释说降级但实际 GetClient 已返回 nil 后安全（逻辑正确，注释优化） | 🟡 轻微 |
| `conf/config.yaml` | 硬编码真实 API Key（安全漏洞） | 🔴 严重（密钥泄漏） |
| `conf/config.yaml` | `default_model: "gpt-5.1"` 是无效模型名，Realtime API 不支持 | 🔴 严重（连接必然失败） |
| `web/audio.html` | `onAudioProcess` 中 `audioDataBuf` 合并后没有帧对齐，直接发送却保留了冗余缓冲变量 | 🟡 逻辑冗余 |

---

### 二、详细改动记录

#### 1. `internal/service/redis/redis.go`

**问题：** `GetClient()` 在 Redis 未启用（`redis.enabled: false`）时直接 `panic`，导致整个服务崩溃。所有调用方（billing、rate、session）均无法处理 Redis 不可用的情况。

**改动：**
- `GetClient()` 改为：返回 `client`（可能为 nil），不再 panic
- 新增 `MustGetClient()`：仅在强依赖场景使用，主动 panic 并说明原因
- 新增 `HSetMap(model, key string, fields map[string]interface{}, expiration time.Duration)`：批量写 Hash 字段，支持 map 参数，内部用 Pipeline 实现原子性，替代原来错误的 `Set(..., map)` 调用
- 所有封装方法（`Set/Get/HSet/HGet/Del`）增加 `nil` 检查，Redis 不可用时静默返回 nil/空值，不 panic

#### 2. `internal/service/session/manager.go`

**问题：** `redis.Set()` 传入了 `map[string]interface{}`，而 go-redis 的 `Set` 命令不支持序列化复合类型，运行时会报错。

**改动：**
- 将 `redis.Set(s.Model, redisKey, map[...]..., ttl)` 改为 `redis.HSetMap(s.Model, redisKey, map[...]..., ttl)`
- `HSetMap` 内部用 Pipeline 一次性写入所有字段并设置过期时间，语义正确

#### 3. `internal/service/billing/billing.go`

**问题：** 5 个函数直接调用 `redis.GetClient()`（返回值无 nil 检查），当 Redis 未启用时触发 nil pointer dereference panic。

**改动（共 5 处）：**

| 函数 | 改动 |
|------|------|
| `RecordAudioDuration` | 调用前 `if client == nil { return nil }` |
| `RecordTokenUsage` | 调用前 `if client == nil { return nil }` |
| `GetUserModuleDuration` | 改用局部变量 `c := redis.GetClient()`，nil 时返回 `0,0,0,nil` |
| `GetSessionUsage` | 改用局部变量，nil 时返回 `0,0,0,nil` |
| `GetDailyDuration` | 改用局部变量，nil 时返回 `0,nil` |
| `GetDailyUsage` | 改用局部变量，nil 时返回 `0,nil` |

#### 4. `conf/config.yaml`

**问题 1：** `models.openai.api_key` 曾硬编码真实 OpenAI API Key（已隐藏），存在密钥泄漏风险。

**改动：** 改为 `"${OPENAI_API_KEY}"`，通过环境变量注入，现有的 `replaceEnvPlaceholders()` 机制会自动替换。

**问题 2：** `models.openai.default_model: "gpt-5.1"` 是无效模型名，OpenAI Realtime API 仅支持 `gpt-4o-realtime-preview`，连接必然被拒绝。

**改动：** 改为 `"gpt-4o-realtime-preview"`（Realtime API 正确模型名）。

**问题 3：** `models.azureai.api_key` 硬编码了真实 Azure API Key。

**改动：** 改为 `"${AZURE_OPENAI_API_KEY}"`。

**问题 4：** `models.azureai.default_model: "gpt-5.1"` 同上无效。

**改动：** 改为 `"gpt-4o-realtime-preview"`。

#### 5. `web/audio.html`

**问题：** `onAudioProcess` 中声明了 `audioDataBuf`（帧对齐缓冲），但 `combined` 合并后直接发整块（ScriptProcessor 每次回调本身已是固定大小 `SCRIPT_PROC_SIZE` 个样本），帧对齐逻辑是冗余的，反而增加复杂度。

**改动：**
- 删除 `combined` 拼接逻辑，直接 `float32ToPcm16(resampled)` → `buf2base64()` → `wsSend()`
- 删除 `stopRecording` 中的 `audioDataBuf = new Int16Array(0)` 重置（变量已移除）
- 全局变量 `audioDataBuf` 声明也随之移除

---

### 三、调试启动记录（2026-03-27 18:40）

**构建：**
```
go build -o server_debug.exe ./cmd/server/main.go
# 输出：Build OK（零警告零错误）
```

**启动命令：**
```cmd
set OPENAI_API_KEY=<key>
server_debug.exe
```

**启动日志（logs/startup.log）：**
```
未找到环境配置 config_dev.yaml，跳过   ← 已知，Viper MergeInConfig 路径问题，不影响运行
已加载模型配置: azureai.yaml
已加载模型配置: openai.yaml
[TozoAI] 环境: dev | 服务地址: :8096 | 模型数: 2
INFO  global  Redis 初始化成功 {"addr": "127.0.0.1:6379", "db": 0}
[GIN-debug] GET /health
[GIN-debug] GET /test/generate-token
[GIN-debug] GET /api/redis/keys
[GIN-debug] GET /web/*filepath
[GIN-debug] GET /ws/realtime/openai
INFO  global  服务启动中... {"addr": ":8096", "env": "dev", "jwt_enabled": true, "rate_limit_enabled": true, "fallback_enabled": false}
```

**端口验证：**
```
TCP  0.0.0.0:8096  LISTENING  PID=28960
```

**接口验证：**
```
GET /health → {"status":"ok","time":"2026-03-27T18:48:07+08:00"} ✅
GET /test/generate-token?userId=test001 → {"code":200,"token":"eyJ..."} ✅
```

**浏览器测试页面：**
```
http://127.0.0.1:8096/web/audio.html  ← 已通过 cmd start 打开
```

**已知遗留问题（不影响运行）：**
- `config_dev.yaml` 未被 Viper 找到：原因是 Viper 的 `SetConfigName(config_dev)` 后，AddConfigPath 只有 `./conf`，而 `config.dev.yaml` 文件名与 Viper 期望的 `config_dev.yaml` 不一致（点和下划线差异）。下次修复时将 `config.dev.yaml` 重命名为 `config_dev.yaml`，或改用 `v.SetConfigFile()` 指定完整路径。

---

## 变更记录（2026-03-27 第二次）

### `.tmp/xiaozhi_opus_app.js` — 逻辑补全

| 问题 | 修复内容 |
|------|---------|
| `AudioContext` 在顶层 `new`，Windows 10 Chrome 自动 suspend | 改为延迟到 `initAudioContext()` 函数内创建，必须由用户手势触发 |
| `startRecording` 直接 `continueStartRecording()`，未检查 AudioContext 是否就绪 | 新增 `if (!audioContext)` 提前拦截，提示先初始化 |
| `processAudioData` 无重采样逻辑 | 新增 `resampleLinear()`，当浏览器实际采样率 ≠ `SAMPLE_RATE` 时自动线性插值 |
| ScriptProcessor 连接到 `audioContext.destination` 导致回声 | 改连 `createMediaStreamDestination()`（静音虚拟输出） |
| 缺少麦克风测试功能 | 新增 `testMicrophone()`：录制 3 秒原始 PCM → 计算 RMS → 自动回放验证 |
| 缺少 `setStatus` 工具函数 | 新增，统一状态更新出口 |
| DOM 元素在顶层获取（脚本在 `<body>` 前时为 null） | 改到 `DOMContentLoaded` 回调内获取 |
| AudioWorklet 内 `workletCode` 类名与 PCM 用途不一致 | 类名改为 `PcmRecorderProcessor`，注释说明用途 |

### `web/audio.html` — Windows 10 兼容 + 功能补全

| 问题 | 修复内容 |
|------|---------|
| 无"初始化音频"前置步骤，Windows 10 Chrome 录音静默失败 | 新增 Panel 0「音频初始化」，`btnInitAudio` 在用户手势内同时创建 `recCtx`（录音）和 `playCtx`（播放）两个 AudioContext |
| 缺少麦克风测试 | 新增"测试麦克风（3秒）"按钮，录 3 秒 → RMS 计算 → 自动回放 |
| AudioWorklet 未实现，只用了 ScriptProcessor | 新增 `createWorkletProcessor()`，用 Blob URL 内联 `PcmRecorder` WorkletProcessor，无需外部 JS 文件；失败自动降级 ScriptProcessor |
| ScriptProcessor 连接到 `destination` 导致回声 | 改连 `createMediaStreamDestination()`，AudioWorklet 同理 |
| `playCtx` 在 `initAudio` 用户手势内同步创建 | 确保播放 AudioContext 也在手势内激活，避免 Windows 10 自动 suspend |
| 导航链接使用绝对路径 `/web/index.html` | 改为相对路径 `index.html` / `redis.html` |
| 无状态点显示音频初始化和麦克风测试进度 | 新增 `dotAudio` / `dotMicTest` 状态点 |
| 统计栏缺少"处理器类型"字段 | 新增 `statProc`，显示当前是 AudioWorklet 还是 ScriptProcessor |
| 连接按钮未检查 `audioCtxReady` | 连接前拦截，提示先初始化音频 |
| Windows 10 提示缺失 | 新增黄色提示横幅，初始化后自动隐藏 |

---

## 一、需求概览

| # | 需求 | 状态 |
|---|------|------|
| 1 | JWT 认证增加 `enabled` 开关 | 已完成 |
| 2 | 精简配置结构 | 已完成 |
| 3 | HTTP 降级接口全局配置开关 | 已完成 |
| 4 | 限流全局开关 + 各模型独立速率配置 | 已完成 |
| 5 | 双端心跳检测（App↔Go, Go↔OpenAI）+ 断线重连逻辑 | 已完成 |
| 6 | 全局统一 JSON 响应格式 | 已完成 |
| 7 | 识别多余/不需要的文件 | 已完成 |
| 8 | 计费按模型/模块拆分，音频时长统计 | 已完成 |
| 9 | 生成重构总结报告 | 本文件 |
| 10 | 新增 Web 测试面板（WS 连接/心跳/消息测试） | 已完成 |
| 11 | 全局 requestId 贯穿日志链路 | 已完成 |
| 12 | Web 面板支持 userId 参数传入 | 已完成 |
| 13 | 新增 Redis 监控面板（Layui 表格） | 已完成 |

---

## 二、本次修改记录（2026-03-27）

### 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `internal/provider/openai/client_ws.go` | 重写 | 修复心跳逻辑、改进日志、完善注释 |
| `internal/service/session/manager.go` | 重写 | 删除 heartbeatLoop、修复会话退出流程 |

---

### 修改1：删除 session.heartbeatLoop — 修复致命并发写 BUG

**问题描述：**

原有架构中存在两层心跳，造成致命 BUG：

1. **Provider 层**（`client_ws.go`）：
   - `writePump` 定时发 WS Ping → 是 AppConn 的**唯一写入者**
   - `readPump` 设 PongHandler + ReadDeadline → 检测 App 存活

2. **Session 层**（`session/manager.go`）：
   - `heartbeatLoop` 直接调用 `s.AppConn.WriteJSON(heartbeat)` — **并发写 AppConn！**
   - 同时调用 `s.AppConn.SetReadDeadline()` — 与 `readPump` **冲突**

**问题：**
- gorilla/websocket 的 Conn 不支持并发写，`writePump` 和 `heartbeatLoop` 同时写会导致 **panic**
- 两层 `SetReadDeadline` 互相覆盖，导致心跳超时判断不稳定

**修复：**
- **删除 `heartbeatLoop`**：心跳完全由 Provider 层的 `writePump`/`readPump` 负责
- 确保 AppConn 只有 `writePump` 一个写入者
- Redis 会话元数据更新移到 `Start()` 初始化阶段

---

### 修改2：修复 session.Start 会话退出流程

**问题描述：**

原有 `session.Start()` 流程：
```go
func (s *Session) Start(ctx context.Context) {
    // ... 启动 heartbeatLoop（已删除）
    if err := s.Provider.HandleWS(ctx, s.AppConn); err != nil {
        // 错误时直接写 AppConn → 与 writePump 并发写！
        _ = s.AppConn.WriteJSON(map[string]interface{}{...})
        s.Close()
        return
    }
    <-s.done // 永远阻塞！因为 HandleWS 返回后 done 不会被 close
}
```

**问题：**
1. HandleWS 错误时直接写 AppConn → writePump 可能还在运行 → **并发写**
2. HandleWS 正常返回后 `<-s.done` 永远阻塞 → **死锁**
3. `done` channel 只在 `Close()` 中 `close()`，但 `Close()` 由 defer 在 handler 中调用

**修复：**
```go
func (s *Session) Start(ctx context.Context) {
    // 记录 Redis 元数据
    // 直接调用 HandleWS（阻塞直到所有协程退出）
    if err := s.Provider.HandleWS(ctx, s.AppConn); err != nil {
        s.log.Error("Provider WS 处理异常退出", zap.Error(err))
    }
    // HandleWS 返回 = 会话结束（不再等待 <-s.done）
}
```

- 移除 `<-s.done` 死锁
- 移除错误时对 AppConn 的直接写（由 Provider 内部处理）
- 移除 `done` channel（不再需要）

---

### 修改3：心跳秒数全部从配置文件读取

**确认配置读取路径：**

```
config.yaml
  → models.openai.realtime.app_ping_interval  (如 "30s")
  → models.openai.realtime.app_pong_timeout   (如 "60s")
  → models.openai.realtime.api_read_timeout   (如 "120s")
  → models.openai.realtime.reconnect_max_retries (如 3)
  → models.openai.realtime.reconnect_delay    (如 "1s")

conf.ModelConfig.Realtime
  → OpenAIConfig.GetAppPingInterval()  → writePump
  → OpenAIConfig.GetAppPongTimeout()   → readPump
  → OpenAIConfig.GetApiReadTimeout()   → recvPump
  → OpenAIConfig.GetMaxRetries()       → reconnect
  → OpenAIConfig.GetReconnectDelay()   → reconnect
```

App↔Go 和 Go↔OpenAI 的心跳秒数**分别配置、分别读取**，无硬编码常量。

---

### 修改4：改进日志记录逻辑

| 位置 | 原问题 | 修复 |
|------|--------|------|
| readPump 读取失败 | 所有错误统一用 `Warn` | 区分：正常关闭→`Info`，异常断开→`Warn`，心跳超时→`Info`（附超时时间） |
| recvPump 读取 OpenAI 失败 | 正常关闭也用 `Warn` | 区分正常关闭→`Info`，异常→`Warn`（附 read_timeout） |
| handleOpenAIMessage 收到错误事件 | 无日志 | 添加 `Warn` 日志记录错误事件类型 |
| safeSend channel 满 | 只记录 data len | 增加 `chan_cap` 字段帮助排查缓冲区问题 |
| writePump 发 Ping | 无日志 | 添加 `Debug` 日志记录 Ping 发送和间隔 |
| reconnect 等待期间 | 不检查 context 取消 | 添加 `select` 检查 `ctx.Done()`，避免上下文已取消时无效等待 |
| session.Close Token 统计 | total=0 也记录 | 改为 total>0 时才记录，减少无意义日志 |
| session.Start 错误处理 | 直接写 AppConn（并发写） | 移除，只记日志（由 Provider 内部处理错误通知） |
| processPump 处理失败 | 只记 error | 增加 msg_type 和 msg_len 字段，便于排查 |
| handleAppMessage default | 无日志 | 添加 Debug 日志记录未知消息类型 |

---

### 修改5：全局 requestId 贯穿日志链路（需求11）

**问题**：原有日志只有 `session_id` 和 `user_id`，当同一用户多次连接时，无法区分是哪次连接的日志。

**方案**：每次 WS 连接时生成全局唯一 `requestId`（格式：`req_{userID}_{时间戳hex}`），贯穿 handler → session → client_ws 全链路。

**修改文件：**

| 文件 | 修改内容 |
|------|----------|
| `internal/handler/openai_handler.go` | 新增 `generateRequestID()`，创建带 `request_id`+`user_id` 的 logger |
| `internal/service/session/manager.go` | `NewSession` 增加 `requestID` 参数，logger 携带 `request_id` |
| `internal/provider/provider.go` | 新增 `LoggerProvider` 接口（可选，`SetLogger(*zap.Logger)`） |
| `internal/provider/openai/client_ws.go` | 实现 `SetLogger` 方法，接收带 `request_id` 的 logger |

**日志效果：**
```json
{
  "level": "INFO",
  "msg": "会话启动",
  "request_id": "req_1001_192f3a5b8c0",
  "session_id": "01968e4a-...",
  "user_id": "1001",
  "model": "openai"
}
```

**日志检索方式：**
```bash
# 按 requestId 检索某次连接的完整链路
grep "req_1001_192f3a5b8c0" logs/openai/*.log

# 按 userId 检索某用户的所有连接
grep "user_id.*1001" logs/openai/*.log
```

---

### 修改6：Web 面板支持 userId 参数传入（需求12）

**修改：**
- `web/index.html`: 新增 userId 输入框（默认 1001）
- `web/ws-test.js`: 从 URL `?userId=xxx` 自动读取并填充；Token 请求带 `?userId=xxx`
- `cmd/server/main.go`: `/test/generate-token` 接口支持 `?userId=xxx` 参数

**使用方式：**
```
http://127.0.0.1:8096/web/?userId=1001
```

---

### 修改7：新增 Redis 监控面板（需求13）

**新增文件：**

| 文件 | 说明 |
|------|------|
| `web/redis.html` | Redis 监控页面（Layui 表格组件） |
| `internal/handler/redis_handler.go` | Redis 数据 API（GET /api/redis/keys） |

**功能：**
- 使用 Layui table 组件展示所有 Redis key
- 显示 key 名称、类型（string/hash/list/set/zset 彩色标签）、TTL、值（JSON 格式）
- 自动刷新（3s/5s/10s/30s 可选）
- 统计卡片（Key 总数、String 数、Hash 数）
- 支持 SCAN 匹配模式过滤（如 `session:*`）
- 导航链接可跳转到 WS 测试面板

**使用方式：**
```
http://127.0.0.1:8096/web/redis.html
```

**API 接口：**
```
GET /api/redis/keys?pattern=*&count=200
```

---

### 修改8：新增 Web 测试面板

**新增文件：**

| 文件 | 说明 |
|------|------|
| `web/index.html` | WS 测试主页面（连接配置、状态面板、心跳测试、消息发送、实时日志） |
| `web/style.css` | 暗色主题样式（GitHub Dark 风格） |
| `web/ws-test.js` | WS 测试客户端逻辑（连接管理、心跳模拟、事件解析、日志记录） |

**配套代码修改：**

| 文件 | 修改 |
|------|------|
| `cmd/server/main.go` | 新增 `public.Static("/web", "./web")` 路由，服务静态文件；token 接口返回增加 `code: 200` |
| `internal/middleware/auth.go` | JWT 中间件增加 URL 参数 `token` 兼容（浏览器 WS 不支持自定义 Header） |

**使用方式：**
```bash
# 1. 启动 Go 服务
go run cmd/server/main.go

# 2. 打开浏览器
http://127.0.0.1:8096/web/

# 3. 点击"自动获取 Token" → 点击"连接"
# 4. 观察连接状态、心跳事件、消息收发
```

**测试功能覆盖：**
- WS 连接建立与断开
- JWT Token 自动获取（通过 URL 参数传递）
- WS 协议层 Ping/Pong 心跳（浏览器自动响应）
- 客户端应用层心跳模拟
- OpenAI Realtime 事件发送（session.update / response.create 等）
- 响应事件解析与展示（begin/end/text_delta/audio_delta 等）
- 心跳超时模拟（断开连接模拟 App 崩溃）
- 手动重连测试
- 实时日志（分级别、可过滤心跳日志、可导出）

---

### 修改6：确认 IDE 报错问题

| 问题 | 结论 |
|------|------|
| `client_ws.go` 中 `"encoding/json"` 未使用 | **文件中不存在该 import**（第43-60行），只有 `encoding/base64`。建议 IDE 执行 Invalidate Caches 清除索引缓存 |
| `billing.go` 中 `redis.Nil` 未解析引用 | **代码已正确使用 `goredis.Nil`**（第127、202行），`go build` 通过。建议 IDE 清除缓存后重新索引 |

---

## 三、心跳+重连架构总结

### 最终架构（修复后）

```
                     心跳检测                          心跳检测
  App ←──Ping/Pong──→ Go服务(writePump/readPump) ←──ReadTimeout──→ OpenAI
       app_ping_interval                              api_read_timeout
       app_pong_timeout
```

### 职责分工

| 组件 | 职责 | 写入 AppConn |
|------|------|-------------|
| writePump | 发 sendChan 消息 + 定时发 Ping | ✅ 唯一写入者 |
| readPump  | 读 App 消息 + PongHandler + 超时检测 | ❌ 不写 |
| processPump | 解析 App 消息 → 转发 OpenAI | ❌ 不写 |
| recvPump | 读 OpenAI + 重连 + 包装响应 | ❌ 通过 sendChan 间接写 |
| session.Start | 生命周期管理 + Redis 元数据 | ❌ 不写 |

### 退出流程

**场景 A：App 心跳超时（App→Go 断开）**
1. readPump: ReadDeadline 超时 → ReadMessage 返回 error
2. readPump: 调用 cancel() → ctx.Done() 触发
3. writePump/processPump/recvPump: 检测到 ctx.Done() → 退出
4. HandleWS: wg.Wait() 返回
5. session.Start: HandleWS 返回 → 记录日志
6. openai_handler.go: defer sess.Close() → 关闭 OpenAI 连接 + 更新 Redis

**场景 B：Go→OpenAI 断开**
1. recvPump: ReadMessage 返回 error → 尝试 reconnect()
2. 重连成功 → continue 继续读取（计数器在 Connect 成功时重置）
3. 重连失败（超过 max_retries）→ 发 reconnect_required → cancel()
4. 后续与场景 A 步骤 3-6 相同

---

## 四、文件改动清单

### 新增文件

| 文件 | 说明 |
|------|------|
| `pkg/response/response.go` | **全局统一响应格式**（需求6）：StandardResponse 结构体，事件类型枚举 |
| `internal/provider/openai/init.go` | Provider 工厂注册（init() 自动注册） |

### 重写文件

| 文件 | 改动要点 |
|------|----------|
| `conf/config.go` | 新增 `JWT.Enabled`、`RateLimit.Enabled`、`Fallback.Enabled`、`Billing.Enabled`；新增 `Endpoint` 字段 |
| `conf/config.yaml` | 新增 `rate_limit`/`fallback`/`billing` 配置节；`jwt` 增加 `enabled` 字段 |
| `conf/config.dev.yaml` | 新增 `rate_limit.enabled: false`（开发关闭限流）、`fallback.enabled: true` |
| `conf/config.prod.yaml` | 新增 `rate_limit.enabled: true`、`billing.enabled: true` |
| `conf/models/openai.yaml` | 移除顶层 `name` 字段（不在 ModelConfig 中） |
| `conf/loader.go` | 无改动（保持原有加载逻辑） |
| `cmd/server/main.go` | 空导入触发工厂注册；条件注册中间件和路由；新增 /health 路由 |
| `internal/middleware/auth.go` | 新增 `JWT.Enabled` 检查；禁用时设置 `user_id=anonymous`；读取 `ExpireHours` |
| `internal/middleware/rate.go` | 新增 `RateLimit.Enabled` 检查；禁用时直接放行 |
| `internal/handler/openai_handler.go` | 新增 `Fallback.Enabled` 检查；安全类型断言 |
| `internal/provider/provider.go` | 精简 Provider 接口（移除 HandleClientEvent/HandleServerEvent） |
| `internal/provider/openai/client_ws.go` | **核心重写**：修复心跳并发写BUG；改进日志级别区分；完善重连 context 检查 |
| `internal/provider/openai/config.go` | OpenAI 配置封装：GetWsURL/GetHeartbeatInterval/GetMaxRetries |
| `internal/provider/openai/events_client.go` | 客户端事件处理器（类型化分发） |
| `internal/provider/openai/events_server.go` | 服务端事件处理器（错误增强 + Token 统计） |
| `internal/service/billing/billing.go` | **重写**：新增音频时长计费、模块拆分（chat/meeting/translation）、配置开关 |
| `internal/service/session/manager.go` | **重写**：删除 heartbeatLoop 并发写BUG；修复 Start() 死锁；优化日志 |

### 无改动文件

| 文件 | 说明 |
|------|------|
| `internal/logger/logger.go` | 日志系统无变动 |
| `internal/service/redis/redis.go` | Redis 封装无变动 |
| `internal/rate/factory.go` | 限流工厂无变动 |
| `pkg/utils/utils.go` | 工具函数无变动 |
| `pkg/errors/common.go` | 错误码定义无变动 |

---

## 五、不需要/多余的文件（需求7）

| 文件 | 状态 | 原因 |
|------|------|------|
| `internal/ws/client.go` | **可删除** | 预留的通用 Hub 模式实现，当前主流程不使用。processPump 的业务逻辑是空的。 |
| `internal/ws/handler.go` | **可删除** | 预留的 Hub 路由处理器，未在 main.go 中注册任何路由 |
| `internal/ws/hub.go` | **可删除** | WS Hub 广播模式，当前业务不需要广播功能 |
| `internal/ws/message.go` | **可删除** | xiaozhi 消息格式定义，与当前 OpenAI Realtime 协议不匹配 |
| `internal/types/types.go` | **可删除** | 空文件，未定义任何类型 |
| `internal/handler/azureai_handler.go` | **可删除** | 空文件，仅声明 package |
| `internal/provider/azureai/client.go` | **可删除** | 空文件，仅声明 package |
| `internal/provider/azureai/config.go` | **可删除** | 空文件，仅声明 package |
| `internal/provider/azureai/response.go` | **可删除** | 空文件，仅声明 package |
| `pkg/errors/azureai_error.go` | **可删除** | 空文件，仅声明 package |
| `pkg/errors/openai_errors.go` | **可删除** | 空文件，仅声明 package |
| `conf/models/azureai.yaml` | **可删除** | 空文件，无任何配置内容 |
| `internal/rate/factory.go` | **建议保留但标记为备用** | 与 middleware/rate.go 功能重叠，但可供非中间件场景调用 |
| `REFACTOR_REPORT.md` | **可删除** | 上一次重构的报告，被本文件取代 |

> **建议**：`internal/ws/` 整个目录可以安全删除。如果未来需要多客户端广播功能，可以重新实现。

---

## 六、各需求详细改动说明

### 需求1：JWT 认证增加 `enabled` 开关

**配置变更**：
```yaml
jwt:
  enabled: true       # ← 新增
  secret: "..."
  issuer: "TozoAI"
  expire_hours: 24
```

**代码变更**：
- `conf/config.go` → `JWT` 结构体新增 `Enabled bool`
- `internal/middleware/auth.go` → 中间件入口检查 `conf.Global.JWT.Enabled`，禁用时设 `user_id=anonymous` 并跳过验证

### 需求2：精简配置结构

**改动**：
- 保持 `config.yaml` 作为公共基础配置（所有环境的默认值）
- `config.dev.yaml` / `config.prod.yaml` 只覆盖差异项
- `conf/models/*.yaml` 各模型独立配置文件
- 配置加载顺序：`config.yaml` → `config_{env}.yaml` → `models/*.yaml` → 环境变量

### 需求3：HTTP 降级接口全局配置开关

**配置变更**：
```yaml
fallback:
  enabled: false    # 默认关闭
```

**代码变更**：
- `conf/config.go` → 新增 `Fallback` 结构体
- `cmd/server/main.go` → 条件注册 `/v1/chat/completions` 路由
- `internal/handler/openai_handler.go` → `OpenAIFallbackHandler` 检查开关

### 需求4：限流全局开关 + 各模型独立速率配置

**配置变更**：
```yaml
rate_limit:
  enabled: true    # 全局限流开关

models:
  openai:
    rate_rps: 10   # 各模型独立配置
    rate_burst: 20
  azureai:
    rate_rps: 5
    rate_burst: 10
```

**代码变更**：
- `conf/config.go` → 新增 `RateLimit` 结构体
- `internal/middleware/rate.go` → 入口检查 `conf.Global.RateLimit.Enabled`

### 需求5：双端心跳检测 + 断线重连

**架构**：
```
App ←──Ping/Pong──→ Go服务(writePump/readPump) ←──Read/Write──→ OpenAI
```

**App↔Go 心跳**（配置项：`app_ping_interval`、`app_pong_timeout`）：
- `writePump` 按配置间隔发送 WS Ping
- `readPump` 设置 PongHandler，按配置超时时间检测 App 存活
- App 断开时：readPump 检测到错误 → `cancel()` → 所有协程退出 → `session.Close()` 关闭 OpenAI 连接

**Go↔OpenAI 心跳**（配置项：`api_read_timeout`）：
- `recvPump` 持续读取 OpenAI 消息
- 读取错误时调用 `reconnect()` 尝试重建连接
- 重连失败发送 `reconnect_required` 事件给 App

**重连流程**：
1. App 断开 → Go 服务检测到 → 关闭 OpenAI 连接
2. App 重新连接 → 新的 handler 调用 → 创建新 Session + 新 Provider
3. App 发送历史对话（通过 `conversation.item.create` 事件）
4. Go 服务透传给新的 OpenAI 连接

### 需求6：全局统一 JSON 响应格式

**新增文件**：`pkg/response/response.go`

**响应结构**：
```json
{
  "code": 0,
  "response": "text_delta",
  "response_id": "uuid-v7",
  "content": "...",
  "input_timestamp": 1774598019000,
  "resp_timestamp": 1774598019050
}
```

**事件类型映射**：
| OpenAI 事件 | response 值 |
|---|---|
| session.created | session_created |
| response.created | begin |
| response.done | end |
| response.text.delta | text_delta |
| response.audio.delta | audio_delta |
| response.audio_transcript.delta | transcript_text_delta |
| error | error |

### 需求7：多余文件分析

见上方「五、不需要/多余的文件」表格。共 14 个文件/目录可安全删除。

### 需求8：计费按模型/模块拆分 + 音频时长统计

**新增模块常量**：
```go
ModuleChat        = "chat"        // 聊天模块
ModuleMeeting     = "meeting"     // 会议模块
ModuleTranslation = "translation" // 翻译模块
```

**Redis Key 结构**：
```
# 用户模块音频时长（Hash）
billing:duration:{model}:{module}:{userID}
  - input_audio_ms:   累计输入音频时长（毫秒）
  - output_audio_ms:  累计输出音频时长（毫秒）
  - total_audio_ms:   累计总音频时长
  - session_count:    会话次数
  - last_session_id:  最后一次会话ID
  - last_updated:     最后更新时间戳

# 每日模型音频总时长
billing:daily_duration:{model}:{date}
```

**配置开关**：
```yaml
billing:
  enabled: true    # 全局计费开关
```

**API**：
- `RecordAudioDuration(model, module, userID, sessionID, inputMs, outputMs)` — 记录音频时长
- `GetUserModuleDuration(model, module, userID)` — 查询用户模块累计时长
- `GetDailyDuration(model, date)` — 查询每日总时长
- 原有 `RecordTokenUsage` / `GetSessionUsage` 保留

---

## 七、配置文件结构总览

```
conf/
├── config.yaml          # 公共基础配置（所有环境默认值）
├── config.dev.yaml      # 开发环境覆盖
├── config.prod.yaml     # 生产环境覆盖
├── loader.go            # 配置加载逻辑
├── config.go            # 配置结构体定义
└── models/
    ├── openai.yaml      # OpenAI 模型配置
    └── azureai.yaml     # Azure AI 模型配置（空，待实现）
```

---

## 八、项目目录结构（清理后建议）

```
TozoAI-Chat-Api/
├── cmd/server/main.go           # 服务入口
├── conf/                         # 配置层
├── internal/
│   ├── handler/                  # HTTP/WS 路由处理器
│   │   └── openai_handler.go
│   ├── logger/                   # 日志系统
│   │   └── logger.go
│   ├── middleware/               # 中间件
│   │   ├── auth.go              # JWT 鉴权
│   │   └── rate.go              # 限流
│   ├── provider/                 # 模型 Provider
│   │   ├── provider.go          # 接口 + 工厂
│   │   └── openai/
│   │       ├── init.go          # 工厂注册
│   │       ├── client_ws.go     # WS 客户端核心
│   │       ├── config.go        # 配置封装
│   │       ├── events_client.go # 客户端事件处理
│   │       └── events_server.go # 服务端事件处理
│   └── service/
│       ├── billing/billing.go   # 计费服务
│       ├── redis/redis.go       # Redis 封装
│       └── session/manager.go   # 会话管理
├── pkg/
│   ├── errors/common.go         # 错误码
│   ├── protocol/openai/         # OpenAI Realtime 协议定义
│   │   ├── types.go
│   │   ├── client_events.go
│   │   └── server_events.go
│   ├── response/response.go     # 全局响应格式（新增）
│   └── utils/utils.go           # 工具函数
└── go.mod
```

> 建议删除：`internal/ws/`、`internal/types/`、`internal/rate/`、`internal/provider/azureai/`、`internal/handler/azureai_handler.go`、`pkg/errors/azureai_error.go`、`pkg/errors/openai_errors.go`、`conf/models/azureai.yaml`
