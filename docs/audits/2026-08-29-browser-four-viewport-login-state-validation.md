# 浏览器四视口登录态验收报告（2026-08-29，真实浏览器执行）

> 承接：进度文档 §四.A（Phase 2 Task 10/11）与 §四.D（Phase 7）中长期标注
> `BLOCKED_BY_BROWSER_POLICY` 的浏览器验收项。此前 2026-08-22 仅交付过公开登录页
> 无头截图冒烟（16 张，`four-viewport-20260822-002017/`）。本轮使用 ZCode 浏览器控制
>（真实内核 IAB，非无头 CLI）完成**登录态 + 四视口**的首次真实浏览器验收。

---

## 一、执行环境

| 项 | 值 |
| --- | --- |
| 服务 | `php artisan serve` @ `http://127.0.0.1:8098`（执行后已停止，无孤儿进程） |
| 数据库 | `co_crmv5_test` 隔离测试库（`prepare-test-database.php` + FrontDemoDataSeeder 演示账号，正式库零写入） |
| 账号 | 后台 `admin@crmv5.com`（superadmin）；前台 `agent@test.com`（王坤，尊贵的代理商） |
| 视口 | 1440x900 / 1280x720 / 768x1024 / 390x844（`setViewportSize` 真实视口切换） |
| 证据目录 | `docs/audits/four-viewport-20260829-browser/`（**28 张 PNG，3.3MB**） |

## 二、覆盖矩阵（28 张截图）

| 家族 | 页面 | 1440x900 | 1280x720 | 768x1024 | 390x844 |
| --- | --- | --- | --- | --- | --- |
| admin layui | `/admin/login` | ✅ | ✅ | ✅ | ✅ |
| admin layui | `/admin/dashboard`（登录态） | ✅ | ✅ | ✅ | ✅ |
| admin layui | `/admin/agents`（登录态模块页抽样） | ✅ | — | — | — |
| front layui | `/front/login` | ✅ | ✅ | ✅ | ✅ |
| front layui | `/front/dashboard`（登录态） | ✅ | ✅ | ✅ | ✅ |
| front layui | `/front/flow`（登录态模块页抽样，8 tab） | ✅ | — | — | — |
| admin crmui | `/admin-crmui/login` | ✅ | ✅ | ✅ | ✅ |
| admin crmui | `/admin-crmui/dashboard`（登录态） | ✅ | — | — | — |
| front crmui | `/front-crmui/login` | ✅ | ✅ | ✅ | ✅ |
| front crmui | `/front-crmui/dashboard`（登录态） | ✅ | — | — | — |

## 三、检查项结果

| 检查项（§四.A 清单子集） | 结果 |
| --- | --- |
| HTTP 正常 | 全部页面 HTTP 200（含登录跳转链） |
| 渲染无横向溢出 / 文字遮挡 | 四视口全部通过；390 窄屏下侧栏自动收起、指标卡纵向堆叠 |
| 登录流程真实可用 | 两 guard 四入口登录均成功并正确跳转 |
| 登录直达 Dashboard（用户诉求 1） | 前台登录后直接落 `/front/dashboard`，**无需手动刷新** |
| 控制台可见元素抽查 | 语言图标下拉、主题下拉（15 主题）、实名认证引导按钮（前台需求 2）、周期切换 7/15/30、多图表（柱状+环形）、8 tab 账户流水 + 靠左统计 div + 真实数据 —— 全部真实渲染 |
| 登录态真实数据 | 前台 dashboard 显示王坤（返佣比例 85.00%、返佣合计 1300.75、直属代理 2 等），流水页 9 条记录 / 金额 13860.75 |

## 四、观察项（非阻塞，如实记录）

1. `/admin-crmui/login` 渲染完成后右侧残留 `Loading...` 文案（表单可正常交互、登录成功后消失）——
   CrmUI 登录壳的加载指示器未在就绪后隐藏，属视觉瑕疵，建议后续在 CrmUI 登录页 JS 初始化完成时移除该节点。
2. 后台 layui 侧栏的权限菜单区（Admin Users 6a23fb27413fd 等）显示的是权限 slug 回退文案，
   与菜单语言包无关的测试库权限数据形态，正式菜单数据下显示中文菜单名。

## 五、诚实边界（不得表述为完整 Task 10 验收）

- 本轮覆盖：**四家族登录页全量（4 视口 × 4）+ 登录态仪表盘（layui 双家族 4 视口、crmui 双家族桌面抽样）+ 两个代表模块页**。
- 未覆盖：全部 ~50 模块页 × 4 视口的完整矩阵、表格行级交互（按钮点击/弹窗）、键盘焦点走查、
  触控目标 ≥44px 系统测量、loading/empty/error 三态人为构造验证。
- 结论表述建议：「浏览器策略阻塞已解除（真实浏览器通道），登录态四视口验收完成首轮抽样（28 张证据），
  完整模块矩阵验收可在此基础上按同一通道分批执行」。

## 六、后续

- 8098 服务已停止；隔离库保留（下次验收可直接复用本通道）。
- 建议下一批：按 §四.A 清单补齐 admin layui 全模块页 4 视口（每模块约 4 张，39 模块约 156 张），
  沿用本轮脚本化通道（每家族约 15-20 分钟）。
