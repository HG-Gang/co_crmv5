# co_crmv5 项目整体进度梳理

> 文档日期：2026-08-29 14:00（§2.21 全量串行 27+1 条失败根因修复与全绿闭环）
> 数据依据：`storage/app/audits/旧项目模块逻辑迁移核验矩阵.json`（2026-08-21 全量收官后重新生成）、`docs/audits/旧项目路由核验证据.json` 中 `admin_gift_business_2026_08_19`、`admin_cancellation_business_2026_08_19`、`admin_position_summary_business_2026_08_21`、`admin_rights_summary_business_2026_08_21`、`admin_open_close_order_business_2026_08_21`、`admin_real_commission_business_2026_08_21`、`admin_batch_credit_business_2026_08_21`、`admin_production_business_2026_08_21`、`admin_funding_ops_misc_business_2026_08_21` 与 `front_disabled_maintenance_test_entries_2026_08_16` 证据组、Phase 2 结果审计报告（2026-08-17）、全量串行测试日志 `full-serial-20260817-113701`（2026-08-17 11:53）、Dashboard Shell UX 结果审计（2026-08-18）

---

## 一、项目背景与目标

| 项 | 内容 |
| --- | --- |
| 新项目（当前工作区） | `co_crmv5`，正式库 `co_crmv5@127.0.0.1:3307`（禁写） |
| 旧项目（只读参照） | `new_co_gmtk_crmv3`，旧正式库 `hank_zl_data@127.0.0.1:3307`（永久只读） |
| 测试库 | `co_crmv5_test@127.0.0.1:3307`（PHPUnit 唯一允许写入的库） |
| 核心目标 | ① 新项目每个模块逻辑 100% 等价替换旧项目，逻辑闭环无误；② 全量优化所有 Blade 页面样式（视觉 C 改造）；③ 前后端不分离，交互全部用 CSS/JS 实现 |
| 技术栈 | Laravel 8.83 / PHP 7.4+ / MySQL 3307 / Blade + Layui + CrmUI 双 UI 家族 / PHPUnit 9.5 / MT4 永久禁用 |

---

## 二、整体进度总览（Phase 0–7 路线图，2026-08-07 制定）

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| **Phase 0** | 安全基线与全量清单（旧源码/路由/业务核验矩阵） | ✅ 完成（2026-08-07） |
| **Phase 1** | 视觉 C 基础层（Layui/CrmUI token、布局、表格、表单、弹窗、响应式） | ✅ 完成（2026-08-07） |
| **Phase 2** | 身份与组织（登录/管理员/用户/代理/大代理/实名/黑名单/权限/数据范围） | 🟡 184/184 已核验，Task 10 浏览器矩阵被浏览器权限阻塞（`DONE_WITH_CONCERNS`） |
| **Phase 3** | 账户与交易（订单/持仓/平仓/风险/MT4） | 🟡 进行中（前台大代理 10 条、后台 FengXian 风控 9 条与 WHS 清零 6 条已写七维证据） |
| **Phase 4** | 资金业务（入金/出金/渠道/汇率/流水/批量导入/优惠券/礼品） | 🟡 进行中（`WithdrawAmountController` 19/19、`WithdrawStatusController` 12/12、`GiftController` 6/6 已核验） |
| **Phase 5** | 返佣与结算（普通/实时返佣/权益/仓位汇总/代理结算） | ⬜ 未开始（包含在剩余 41 条路由范围） |
| **Phase 6** | 运营配置（仪表盘/在线用户/新闻/菜单/权限/系统/分组配置） | 🔄 进行中（News 7 条已闭环，其余包含在剩余 41 条路由范围） |
| **Phase 7** | 隔离迁移与全量验收（schema 空库重放、迁移演练、全量浏览器验证、差异报告） | ⬜ 未开始（迁移 SQL 已构建好，待授权） |

**总体完成度：100%（475/475 路由业务核验全部闭环；2026-08-29 全量串行 4304/4304 exit 0，见 §2.21）**

---

## 三、已完成的核心成果

### 1. 迁移核验矩阵（唯一可信进度账本）

`storage/app/audits/旧项目模块逻辑迁移核验矩阵.json`

| 指标 | 数量 |
| --- | ---: |
| 旧路由（HTTP 方法级）总数 | 475 |
| `verified`（业务闭环已核验） | **475** |
| `needs_manual_business_review`（待人工核验） | **0** |
| `unresolved_legacy_source`（未解决旧源码） | 0 |
| `unmatched_current_route`（未映射新路由） | 0 |

- 路由映射与业务人工核验均已 100% 闭环，无剩余待办。
- 每条记录含 7 个维度证据：旧行为 / 路由映射 / 后端逻辑 / 前端契约 / 权限与范围 / 校验与错误 / 自动化测试。
- Phase 2 精确范围 184/184 verified，0 空分组。

### 2. 自动化测试基线与最新专项回归

- 全量串行：**3641 tests / 71097 assertions / exit 0**，耗时 16:06.388。
- 测试严格隔离：仅写 `co_crmv5_test`；`MT4_ENABLED=false`；正式库禁写；死锁修复后双进程并行 5 轮稳定。
- 测试规模约 700 个文件（Feature 约 640、Unit 约 60），覆盖各模块 Closure/边界/越权/幂等/状态机。
- 11:53 之后已有实现和测试变更，因此 3641 条全量串行结果仍是基线，不冒充当前最新代码的全量回归；最终 Phase 7 必须重新执行完整套件。

### 2.1 出金申请 19 条专项闭环（2026-08-17 21:30）

- `WithdrawAmountController` 对应 19 条 HTTP 方法级路由已全部写入独立七维证据，矩阵从 `365/110` 更新为 `384/91`。
- 完整出金 Feature 回归：`516 tests / 5345 assertions / exit 0`，命令为 `vendor\bin\phpunit --colors=never --filter "(?i)withdraw" tests\Feature`，耗时 04:57.815。
- 矩阵门禁：`AdminWithdrawAmountMatrixClosureTest` 为 `2 tests / 390 assertions`；`Phase2IdentityOrganizationMatrixClosureTest` 为 `19 tests / 428 assertions`；`FrontBigNumberBusinessMatrixClosureTest` 为 `2 tests / 223 assertions`。两个既有模块门禁已改为从 475 行重算全局 summary，不再冻结后续模块的合法增量。
- 旧/现代列表、汇总和导出使用 BCMath 定点计算；大额边界 `99999999999999.99 × 9.99999999 = 999999998999999.90` 已由列表、详情和导出测试锁定。
- OTC 出金 POST 回调只允许专用 `OtcAdapter`，可验签入金 adapter 不能误入；非 POST 方法统一 405，出金、入金及双方 outbox 均零写入。
- 测试隔离已补齐“系统配置原行不存在”的恢复路径，避免测试中断后缺少 `withdrawal_enabled` 导致后续用例级联失败。
- 规格复审为 `✅ Spec compliant`；质量复审发现并关闭 3 个 Important：详情金额禁止 float、CSV 同时防 `= + - @ TAB CR LF` 前缀、`drawpoundage` 行与 footer 统一按 USD `fee` 汇总且不混用 RMB `rmb_fee`；复审为 `✅ Approved`。

### 2.2 前台大代理业务 10 条专项闭环（2026-08-17 21:36）

- 代理列表、持仓汇总、未平仓和已平仓页面及搜索共 10 条路由已写入 `front_big_agent_business_data_2026_08_17` 七维证据组。
- 根代理严格来自当前 `big_agents.sub_agent_ids`，后代只按 `user_infos.parent_id` 展开；列表资金只统计代理本人，持仓按代理本人及完整后代汇总，订单与品种只读取 `user_trades`、`symbol_prices`。
- `FrontBigNumberPositionDataSourceClosureModuleTest`：`14 tests / 148 assertions`；全部 `FrontBig*`：`62 tests / 624 assertions`；`AgentHierarchyClosureRebuildTest`：`22 tests / 102 assertions`。
- CrmUI 已补齐服务端分页、legacy footer 和总数收缩后的合法页重载；批量层级恰好 128 层与单根语义一致，超过 128 层及循环继续失败关闭。
- 代码质量复审两轮提出的分页/footer、最大深度 off-by-one 和页码收缩空页问题均已修复，最终结论为“批准进入 verified”。

### 2.3 默认 Demo Seeder 生产门禁闭环（2026-08-17 22:18）

- `DatabaseSeeder` 始终保留 `InitialDataSeeder` 基础字典初始化；`FrontDemoDataSeeder` 仅允许在 `local/testing` 且 `FRONT_DEMO_SEEDER_ENABLED=true` 经 Laravel 规范化为严格布尔 true 时执行，production 即使误配开关也拒绝。
- 新增 `config/seeding.php`，`.env.example` 明确默认关闭；未执行任何 Seeder 或数据库写命令。
- `DatabaseSeederDemoGateClosureModuleTest` 在 PHP 8.0、PHP 7.4 均为 `8 tests / 15 assertions`，并通过直接执行 `run()` 验证子 Seeder 调用顺序和生产拒绝。
- 同批关闭 PHP 7.4 测试引导中的 `mixed` 伪类型及 `LegacyAdminController` 两处 `match` 语法；全量 PHP 7.4 语法扫描 0 failures，安全门禁测试 `20 tests / 46 assertions`，相关旧出金状态/权限测试 `47 tests / 399 assertions`。
- 规格复审结论 `Spec compliant`，代码质量复审结论 `Approved`。

### 2.4 Dashboard Shell UX 与真实周期统计（2026-08-18 10:49）

- 四套主壳语言/主题入口已统一为图标下拉，当前项具备 check、高亮、`aria-current` 和 `aria-checked`；主题仍由 `CrmTheme` 单一事实源管理。
- 两份 Layui Dashboard 已平面化并清理无消费者渐变 token；新增 7/15/30 天真实统计、四种图表查看方式、44px 图标按钮、主题 token 图表配色和过期请求响应保护。
- Dashboard API 严格拒绝 `days=8`、小数和数组；保留 `monthly_*` 兼容字段，同时返回 `period.days` 明确统计口径。
- 六组目标回归为 `200 tests / 6823 assertions / 0 failures / 0 errors`；PHP/JS 语法和 Blade cache/clear 全部 exit 0。
- 四视口浏览器导航被运行环境安全策略拒绝，状态为 `BLOCKED_BY_BROWSER_POLICY`；隔离测试库服务已停止，`56624` 与 `8098` 均无监听。详见 `docs/audits/2026-08-17-dashboard-shell-ux-round-result.md`。

### 2.5 出金四状态 12 条专项闭环（2026-08-18 16:46）

- `WithdrawStatusController` 的待处理/处理中/已完成/失败四组 GET、Search、Export 共 12 条方法级路由均已写入 `admin_withdraw_status_business_2026_08_17` 七维证据组，矩阵从 `394/81` 更新为 `406/69`。
- 四个 Search 由 URI 强制 `0/1/2/3` 状态，根级、query、`withdraw_source` 和嵌套 `data.status` 不能覆盖；统一返回旧 V2 `code/msg/count/data/totalRow`，并复用 `WithdrawRecordQueryService` 的 scope、分页和 BCMath 定点汇总。
- 四个 Export 归一旧/现代筛选后复用统一安全 CSV；公式前缀 `= + - @ TAB CR LF` 被防护，空结果只保留表头，`user_id<=0` 在规格复审后统一改为 `VALIDATION_FAILED`。
- Layui 与 CrmUI 专属状态页均输出唯一 hidden status，初载、搜索、reset 和 export 共用同一筛选源；汇总页仍保留可切换状态 select。
- 修复后完整出金 Feature：`623 tests / 6001 assertions / exit 0 / 04:59.786`；状态导出 `31/141`、两阶段下载 `7/89`、日期 `2/6`；四个矩阵门禁分别为 `2/347`、`2/390`、`19/428`、`2/223`。
- 独立规格复审最终 `SPEC APPROVED`，独立质量复审 `QUALITY APPROVED`；浏览器四视口仍为策略阻塞，未绕过也未冒充通过。
- 一次带并发代理环境的完整回归曾出现夹具 `AUTO_INCREMENT` 指纹变化；关闭代理测试进程后两次完整串行均通过，根因只可记录为并发污染嫌疑，未修改或放宽数据库指纹门禁。

### 2.6 前台双 UI 三项差异闭环（2026-08-18 17:00）

- 本人仓位汇总新增 `GET /api/front/positions/self-summary`，直接委托旧 `positionSummary2Search()`；CrmUI/Naive 使用独立页面定义，不再以代理树汇总替代本人汇总。
- 账户类型切换新增 `PATCH /api/front/account/trading-profile`，委托既有 `changeAccountSave()`；Layui、CrmUI、Naive 共用切换组件和同一业务事务。
- Big Agent 新增 `/front-naive/big-agent/*` 路由族，与 CrmUI 共用 `BigAgentPageController`、旧 `bigAgents` session 和受限旧 API；中间件按视觉家族返回对应登录页，不接普通用户 JWT。
- 专项回归：仓位汇总 `5 tests / 59 assertions`，账户类型 `11/80`，Big Agent shell `14/226`，全部 `FrontBig*` `72/754`，`CrmUiStackTest` `6/520`；四组 `FrontUiRegressionTest` 为 `18/439`。
- 相关 PHP/JS 语法、路由清单和 Blade cache/clear 均通过；四视口浏览器验收仍保持 `BLOCKED_BY_BROWSER_POLICY`，未表述为通过。

### 2.7 FengXian 风控 9 条专项闭环（2026-08-19 12:00）

- `FengXianManageController` 的盈利页面与 V1/V2 搜索、持仓页面与 V1/V2 搜索、异常 IP 页面/搜索/详情共 9 条方法级路由，已写入 `admin_fengxian_risk_business_2026_08_18` 七维证据组；矩阵从 `406/69` 更新为 `415/60`。
- 盈利风险已从错误的按品种汇总恢复为按业务用户聚合；持仓风险统一读取 `user_trades` 的未平仓、`margin_rate` 与手续费口径；旧 V1 `rows/total`、V2 `code/msg/count/data/totalRow` 和现代 `records` 分页契约分别保留，金额及比例使用 DECIMAL/BCMath 字符串。
- 异常 IP 列表按不同业务用户计数，样例用户 ID 与用户名来自同一用户，合法 IP 筛选在查询前统一 trim；详情继续复用真实登录、交易、入金和出金聚合，并在 scope 后返回每个业务用户一行。
- Layui/CrmUI 已形成盈利、持仓、追保、异常 IP 四模式闭环；CrmUI 正确展开 `records.data` 并读取服务端 `records.total`，loading/empty/error 会清理旧行；Layui 动态 IP 标题已转义，行操作具备 44px 触控高度，IP 详情列提供中英文翻译。
- 新鲜专项回归全部 exit 0：风控关键词 `214 tests / 1385 assertions / 00:13.192`、FengXian `3/6`、IPAddress `5/15`、IP parity `51/145`、风控 UI `18/270`、CrmUI 通用 `17/673`、Legacy UI `64/455`、Global theme `25/3468`、Unified Blade `6/65`、Visual C `18/184`。
- 相关 PHP lint、两份 JS `node --check`、`view:cache` 与 `view:clear` 全部通过。独立规格复审和质量复审完成，未保留 Critical/Important 问题；矩阵门禁确认 `475/415/60/0/0`。
- 四视口浏览器验收仍为 `BLOCKED_BY_BROWSER_POLICY`；未使用 Playwright CLI、Edge 或 CDP 绕过，不能将静态与自动化回归表述为浏览器通过。

### 2.8 NewsInfo 新闻 7 条专项闭环（2026-08-19 13:02）

- `NewsInfoController` 的列表、新增、编辑、搜索、保存、更新、删除 7 条方法级路由已写入 `admin_news_business_2026_08_19` 七维证据组；矩阵从 `415/60` 更新为 `422/53`。
- `NewsController` 是现代列表和写入的唯一业务入口；`LegacyAdminController` 只适配旧 `newsTitle/newsContent/ispush/newsId/newsid`、旧分页和 `rows/total` 或 mutation envelope，不复制新闻 SQL。
- 列表保留旧默认日期、`rows > limit > per_page > 20`、空结果 `rows='' / total=''`；现代接口默认 15，空表单筛选被显式归一化，非法数组、对象、日期、状态和分页继续失败关闭。
- 写入只接受白名单并使用当前管理员作者快照；更新在同一事务内先锁主新闻、再锁目标新闻的活动翻译，使用 PHP `===` 识别字节级镜像，超出翻译列容量时只软删精确镜像并由前台回退主表，异常时整体回滚。
- Layui/CrmUI 已补齐标题、日期、发布状态筛选及 create/update/toggle/delete 独立权限；旧 create/edit 深链接由服务器模式驱动，响应式弹窗、提交 busy 状态、空筛选 reset 和 create/update 共用提交按钮的动态权限均已闭环。
- 新鲜 News Feature 回归为 `162 tests / 1132 assertions / exit 0`；核心 6 文件为 `114 tests / 615 assertions`，7 组通用 UI 门禁为 `141 tests / 5013 assertions`。PHP 7.4.3、JS 语法和 Blade cache/clear 全部 exit 0。
- 后端和 UI 独立复审最终均为 `SPEC APPROVED / QUALITY APPROVED`，Critical/Important/Minor 为 0；五个矩阵门禁分别为 News `2/203`、FengXian `2/259`、WithdrawStatus `2/347`、Phase2 `19/428`、FrontBigNumber `2/223`。
- 四视口浏览器验收仍为 `BLOCKED_BY_BROWSER_POLICY`，未绕过也未表述为通过。下一轮已开始只读审计 `AdminWhsExpZeroController` 的 6 条路由。

### 2.9 WHS 仓位清零 6 条专项闭环（2026-08-19 16:29）

- `AdminWhsExpZeroController` 的页面、扫描、单用户清零、V1/V2 记录查询 5 条后台路由已写入 `admin_whs_exp_zero_business_2026_08_19`；旧缺失 action `GET whstest` 并入 423 维护失败关闭组，矩阵从 `422/53` 更新为 `428/47`。
- `oneKeySearch` 只扫描真实 `user_infos/user_trades/whs_exp_zeros`，为合格普通客户创建 `status=1` 待处理记录并防活动记录重复；V1 保留 `rows/total`，V2 保留 `code/msg/count/data/totalRow`。
- `oneKeyZero` 强制 `account_type=2`，忽略请求伪造的用户名、余额和信用，按旧正负信用公式计算网关金额；记录原子认领为 `status=0`，成功 `0->2` 并回写余额，拒绝或异常 `0->3` 且余额不伪造清零，处理中记录在网关前拒绝。
- `custom_users` 与 `created` 数据范围已覆盖候选、记录、扫描和清零全链路；范围外用户和代理账户均在网关调用前失败关闭。Layui/CrmUI 已具备候选与记录页签、用户/状态/日期筛选、权限控制及原地切换，数据只来自真实 API。
- 修复后 WHS 关键词 Feature 回归为 `58 tests / 390 assertions`；维护运行时 `25/121`、旧路由兼容 `14/1337`、全部矩阵门禁 `32/2011`；相关 PHP lint、Layui JS 语法及 Blade cache/clear 全部 exit 0。
- 独立规格复审最终 `SPEC APPROVED`，独立质量复审 `QUALITY APPROVED`；四视口浏览器状态仍为 `BLOCKED_BY_BROWSER_POLICY`，未表述为通过。下一批为 `GiftController` 6 条。

### 2.10 Gift 礼品 6 条专项闭环（2026-08-19 22:03）

- `GiftController` 的地址列表、发放、两个旧页面、发货列表和两阶段导出共 6 条方法级路由已写入 `admin_gift_business_2026_08_19`，矩阵从 `428/47` 更新为 `434/41`，未解决旧源码和未映射路由仍为 0。
- 地址只读取真实 `user_addresses/user_infos`，强制默认地址、`is_gift_allowed=1` 且排除软删除用户；发放前整批重读 DB 用户和地址，不信任请求快照，混合无效批次零写入，且按旧逻辑不虚构库存或积分联动。
- 发货列表和导出共用 `gift_shipments/admins` 真实查询；created 地址/发放使用 `user_infos.created_by`，记录/导出/更新使用 `gift_shipments.admin_id`。旧导出保留 JSON `data.path` 和一次性 CSV 下载，并具备管理员目录隔离、1 小时 TTL、BOM 与公式注入防护。
- Layui 旧发放页和发货页由 `giftPageMode=send|shipments` 互斥渲染，不初始化非目标 API；CrmUI 将发放收敛到默认地址选择页，表格使用服务端分页，发放提交有 pending 防重，两套 UI 均具备可访问区块/表格标签。
- 新鲜回归全部 exit 0：Gift Feature `102 tests / 938 assertions`，全部矩阵门禁 `34/2186`，AdminDataScope `32/250`，旧后台路由清单/语义 `15/171`，四组通用 UI 门禁 `113/4172`。19 个相关 PHP 文件通过 PHP 7.4.3 lint，两份 JS `node --check`、`view:cache` 和 `view:clear` 均 exit 0。
- 独立规格复审为 `SPEC APPROVED`，三轮独立质量复审最终为 `QUALITY APPROVED`，无保留 Critical/Important/Minor。四视口浏览器仍为 `BLOCKED_BY_BROWSER_POLICY`，未表述为通过；下一批为 `CancellationController` 6 条。

### 2.11 Cancellation 注销申请 6 条专项闭环（2026-08-21 12:10）

- `CancellationController` 的页面、V1/V2 列表、通过、拒绝与 `update_cancel` 共 6 条方法级路由已写入 `admin_cancellation_business_2026_08_19` 七维证据组，矩阵从 `434/41` 更新为 `440/35`，未解决旧源码和未映射路由仍为 0。
- 列表 V1 保留旧 `rows/total` 信封且空结果为空字符串；V2 保留 `code/msg/count/data/totalRow` 且 `totalRow` 为空数组；两者共用 `AdminCancelApplyQueryService` 单一真实读模型：`cancel_applies` 主查询 + AdminDataScope 范围 + 确定性排序 + 分页 1..100，余额批量读取 `user_infos.total_funds`（含软删除已通过用户），持仓按旧口径聚合 `user_trades`（cmd 0-5、`close_time=1970-01-01`、`margin_rate<>0`），无逐行查询与模拟数据。
- 三个审核入口以业务 `cancel_userid` 定位唯一待处理申请后转发 `CancelApplyController@approve/reject` 单一状态机：行锁 + MT4 lock/unlock + 本地事务 + 回滚反向补偿 + `operation_logs`；`update_cancel` 仅接受 `accept_rejection=1/2`，两个决策都强制非空 `cancel_remark`（≤500）并在 MT4 与写库之前失败关闭；成功/失败补回旧 `msg=SUCCESS|FAIL` 与 `col=UPDATESUC|INVALIDUSER|FATALCANOTCONNECT|UPDATEFAIL`。
- Layui/CrmUI 双界面共享同一 API：默认待处理状态、用户 ID/状态/日期筛选与 reset、状态徽章、负余额强调、`status!=0` 隐藏审核按钮；approve/reject 打开同一 500 字上限必填备注弹窗并防重复提交。
- 新鲜回归全部 exit 0：注销关键词 Feature `75 tests / 565 assertions`，UI 门禁 `4 tests / 82 assertions`，全部矩阵门禁 `36 tests / 2371 assertions`（含新 `AdminCancellationBusinessMatrixClosureTest`）；五个既有批次门禁同步改为从 475 行重算全局 summary 并保留各自 verified 下限，不再冻结后续合法增量。修复项包括 misc 注销列表信封断言对齐规范契约与 Blade 注释补齐「status 为空表示全部申请」约定片段。
- 四视口浏览器验收仍为 `BLOCKED_BY_BROWSER_POLICY`，未表述为通过；下一批为 `PositionSummaryController` 5 条。

### 2.12 PositionSummary 后台仓位汇总 5 条专项闭环（2026-08-21 14:40）

- `Admin\PositionSummaryController` 域的页面、V3 搜索、V2 搜索、下级代理搜索与父路径共 5 条方法级路由已写入 `admin_position_summary_business_2026_08_21` 七维证据组，矩阵从 `440/35` 更新为 `445/30`，未解决旧源码和未映射路由仍为 0。
- 三个搜索 URI（`positionSummarySearch`、`v2/positionSummarySearchV2`、`v2/subAgentsListSearchV2`）统一转发 `admin_api_positionSummaryList`；本轮补齐两处旧参数等价缺口：`payloadForLegacyTarget` 新增驼峰 `userName→user_name` 映射（PHP 数组键区分大小写，旧 Blade 筛选此前被忽略），并为三个 URI 注入旧默认日期区间 `2024-01-01 至当天`（旧 V3 控制器缺省行为，现代接口只在显式传入时过滤）。
- `subAgentsSearch+userPId` 语义保持「父级自身 + 直属下级代理」行集合、每行按各自子树汇总真实 `mt4_trades`；`v2/parentPath` 复刻旧 `path/tree` 契约（-> 链路字符串 + 分组配色 span + lay-event/data-user_id）；页面渲染 `admin_layui::position-summary.index`，Layui parseData 拆分 records/summary 并刷新指标卡，CrmUI 共享同一 API。
- 新增 `AdminPositionSummaryLegacyParityClosureModuleTest` 六用例（默认日期排除区间外平仓单、userName 映射、双 V2 URI 行集合、parentPath 结构、页面渲染、非数字 userPId 失败关闭）与 `AdminPositionSummaryBusinessMatrixClosureTest` 门禁。
- 新鲜回归全部 exit 0：position 关键词 Feature `167 tests / 1308 assertions`，参数解析器/旧路由语义/清单门禁 `17 tests / 175 assertions`，全部矩阵门禁 `38 tests / 2522 assertions`；矩阵 MD 再生成后替换字符为 0。Cancellation 门禁同步改为从 475 行重算 summary 并保留 440 下限，不再冻结后续合法增量。
- 四视口浏览器验收仍为 `BLOCKED_BY_BROWSER_POLICY`，未表述为通过；下一批为 `RightsSummaryController` 5 条。

### 2.13 RightsSummary 后台权益汇总 5 条专项闭环（2026-08-21 15:35）

- `RightsSummaryController` 域的页面、列表、详情、导出与下载共 5 条方法级路由已写入 `admin_rights_summary_business_2026_08_21` 七维证据组，矩阵从 `445/30` 更新为 `450/25`，未解决旧源码和未映射路由仍为 0（`confirm_options`/`manual_confirm_options` 与 `test_rights_sum` 此前已 verified，不计入本批）。
- 列表/详情/导出共用 `legacyRightsSettlementRows` 单一真实读模型：`rights_settlements` 表 + `deleted_at` 过滤 + `created_at` 日期区间 + 状态 `0/1/2→0/1` 归一 + AdminDataScopeService 用户级范围；行字段复刻旧 `rightsId/rightsUserId/rightsSumDate/rightsSumStatus/rightsSumRealamt/realamt/rightsSumRemarks` 集合，详情保留 rows/footer 合计契约。
- 导出为安全 CSV 流（attachment; filename=rights_summary_export.csv）并以 `X-Legacy-Export-Message` 头保留旧 `{msg:文件名|FAIL}` 两阶段协议语义；下载路由语义映射 `admin_api_exportRightsSummary` 再生当前筛选导出，关闭旧实现按任意文件名拼接目录的任意读取面。
- 页面渲染 `admin_layui::rights-summary.index`：筛选表单、`admin_rights_summary_export` 导出与 `admin_rights_summary_manual_confirm` 手动确认控件齐备；CrmUI rights-summary 共享同一 API 并有页面契约测试。
- 新鲜回归全部 exit 0：rights 关键词 Feature `34 tests / 201 assertions`，两个旧适配文件 `28 tests / 197 assertions`，全部矩阵门禁 `40 tests / 2671 assertions`；矩阵 MD 再生成后替换字符为 0。本批无代码缺口，属「实现与测试已在位、证据回填闭环」批次。
- 四视口浏览器验收仍为 `BLOCKED_BY_BROWSER_POLICY`，未表述为通过；下一批为 `AdminOpenOrder / AdminCloseOrder` 3+3 条。

### 2.14 AdminOpenOrder / AdminCloseOrder 持仓单与平仓单 6 条专项闭环（2026-08-21 16:15）

- `AdminOpenOrderController`/`AdminCloseOrderController` 域的两个页面、持仓单 V1/V2 搜索与平仓单 V1/V2 搜索共 6 条方法级路由已写入 `admin_open_close_order_business_2026_08_21` 七维证据组，矩阵从 `450/25` 更新为 `456/19`，未解决旧源码和未映射路由仍为 0。
- 四个搜索 URI 统一转发 `admin_api_openPositions`/`admin_api_closedPositions`：未平仓口径为 `close_time` 为空或 0，已平仓口径与其互补；`userId→user_id`、`startdate/enddate→start_date/end_date` 由通用别名归一，`orderId/sym_symbol/orderType/is_coercion` 由现代 TradeController 按旧别名直读（`is_coercion` 按 comment `so` 前缀区分强平），并叠加 AdminDataScopeService 用户级范围。
- 页面渲染 `admin_layui::trades.index`：Layui parseData 适配 records+summary 信封，前端已暴露旧平仓筛选器与 comment 列；CrmUI trades 共享同一 API 并有契约测试。
- 新鲜回归全部 exit 0：订单风险搜索 + MT4 持仓模块 `12 tests / 74 assertions`，trade 关键词 Feature `72 tests / 636 assertions`，全部矩阵门禁 `42 tests / 2848 assertions`；矩阵 MD 再生成后替换字符为 0。本批无代码缺口，属「实现与测试已在位、证据回填闭环」批次。
- 四视口浏览器验收仍为 `BLOCKED_BY_BROWSER_POLICY`，未表述为通过；下一批为 `AdminRealCommissionController` 3 条。

### 2.15 AdminRealCommission 实时返佣 3 条专项闭环（2026-08-21 16:45）

- `AdminRealCommissionController` 域的页面与 V1/V2 搜索共 3 条方法级路由已写入 `admin_real_commission_business_2026_08_21` 七维证据组，矩阵从 `456/19` 更新为 `459/16`，未解决旧源码和未映射路由仍为 0。
- 两个搜索 URI 统一转发 `admin_api_realtimeCommissionList`：返佣口径为 cmd=6 且 comment 命中 DBCN 关键字的余额流水，`userId→user_id`、日期别名由通用归一处理并叠加 AdminDataScopeService 用户级范围；页面渲染 `admin_layui::realtime-commissions.index`，双 UI 共享同一 API。
- 新鲜回归全部 exit 0：realtime 关键词 Feature `32 tests / 199 assertions`，全部矩阵门禁 `44 tests / 2941 assertions`；矩阵 MD 再生成后替换字符为 0。本批无代码缺口，属「实现与测试已在位、证据回填闭环」批次。
- 四视口浏览器验收仍为 `BLOCKED_BY_BROWSER_POLICY`，未表述为通过；下一批为 `BatchCreditController` 3 条。

### 2.16 BatchCredit 批量授信导入 3 条专项闭环（2026-08-21 17:20）

- `BatchCreditController` 域的页面、Excel 上传与搜索共 3 条方法级路由已写入 `admin_batch_credit_business_2026_08_21` 七维证据组，矩阵从 `459/16` 更新为 `462/13`，未解决旧源码和未映射路由仍为 0（重试路由此前已 verified，不计入本批）。
- 本批关闭一处真实缺口：旧 URI `creditImportExcel` 此前无任何端到端测试，新增 `test_legacy_credit_import_excel_accepts_old_file_field_and_creates_pending_records` 锁定旧 file 字段 CSV 上传→整批校验→事务落库（is_synced=0 待同步、批次可追溯、上传循环内零 MT4 网关调用）等价链路。
- 搜索转发 `admin_api_creditImportList` 并保留旧默认日期窗口；页面渲染 `admin_layui::credit-imports.index`，双 UI 共享同一 API。
- 新鲜回归全部 exit 0：导入关键词 Feature `29 tests / 202 assertions`，全部矩阵门禁 `46 tests / 3034 assertions`；矩阵 MD 再生成后替换字符为 0。
- 四视口浏览器验收仍为 `BLOCKED_BY_BROWSER_POLICY`，未表述为通过；下一批为 `AdminProductionController` 3 条。

### 2.17 AdminProduction 产量列表 3 条专项闭环（2026-08-21 18:00）

- `AdminProductionController` 域的页面与 V1/V2 搜索共 3 条方法级路由已写入 `admin_production_business_2026_08_21` 七维证据组，矩阵从 `462/13` 更新为 `465/10`，未解决旧源码和未映射路由仍为 0。
- 两个搜索 URI 统一转发 `admin_api_productionList`：品种行情只读真实 `symbol_prices` 表并叠加数据范围；页面渲染 `admin_layui::productions.index`，双 UI 共享同一 API。
- 新鲜回归全部 exit 0：production 关键词 Feature `22 tests / 128 assertions`，全部矩阵门禁 `48 tests / 3127 assertions`；矩阵 MD 再生成后替换字符为 0。本批无代码缺口，属「实现与测试已在位、证据回填闭环」批次。
- 四视口浏览器验收仍为 `BLOCKED_BY_BROWSER_POLICY`，未表述为通过；下一批合并处理剩余六个小型控制器（WithdrawFlow 2、DepositAmount 2、ExchangeRate 2、UserLoginOnline 2、PayChannel 1、UnDepositAmount 1）。

### 2.18 资金运营六小控制器 10 条收官闭环 —— 475/475 全量核验完成（2026-08-21 19:00）

- `DepositAmountController`/`WithdrawFlowController`/`UnDepositAmountController`/`PayChannelController`/`ExchangeRateController`/`UserLoginOnlineController` 六个控制器的三个流水页面、两个两阶段下载、渠道浏览、汇率浏览/保存与在线用户页面/搜索共 10 条方法级路由，已写入 `admin_funding_ops_misc_business_2026_08_21` 七维证据组，矩阵从 `465/10` 更新为 **`475/0`——全部 475 条旧路由业务核验闭环**，未解决旧源码和未映射路由保持 0。
- 三个流水页面渲染对应模块页且数据只来自现代流水 API（depositFlowList/withdrawFlowList/undepositFlowList）；两个下载路由语义映射 `admin_api_exportDepositFlows`/`admin_api_exportWithdrawFlows` 并拒绝路径穿越、错误角色与跨管理员目录；汇率保存失败关闭零写库；在线用户搜索非数字筛选失败关闭。
- 新鲜回归全部 exit 0：exchange `15 tests / 82 assertions`、channel `46 tests / 377 assertions`、online `23 tests / 116 assertions`、四个遗留适配文件合计 `40 tests / 267 assertions`；全部矩阵门禁 `50 tests / 3420 assertions`（含新收官门禁 `AdminFundingOpsMiscBusinessMatrixClosureTest` 断言全局 475/475/0/0）；矩阵 MD 再生成后替换字符为 0。
- **至此进度文档 §四.B 所列 41 条剩余路由（历经 Cancellation 6、PositionSummary 5、RightsSummary 5、OpenCloseOrder 6、RealCommission 3、BatchCredit 3、Production 3 与收官 10 共八批）全部闭环。**
- 剩余工作仅剩：Phase 2 Task 10/11 四视口浏览器验收（持续 `BLOCKED_BY_BROWSER_POLICY`，未绕过未冒充）与 Phase 7 隔离迁移演练（迁移 SQL 已就绪待授权）。

### 2.19 运维收尾轮：日志轮转、README 重写、浏览器验收交付与 UI 契约修复（2026-08-21 20:20）

- **日志轮转**：`config/logging.php` 默认 stack 通道由无滚动的 `single` 改为 `daily`（保留 14 天）；3344.6MB 的 `laravel.log` 截断归零，末尾 2000 行存档至 `storage/logs/laravel-archive-20260821-before-rotation.log`（293KB）；冒烟写入确认日志已落到 `laravel-2026-08-21.log` 日期文件。
- **README 重写**：根目录 `README.md` 由旧 Go WebSocket 项目说明重写为当前 Laravel CRM 文档（技术栈、三库写入约束、快速开始、目录结构、旧路由兼容层、测试约束、运维命令、安全红线、文档索引）。
- **浏览器验收交付**：2026-08-21 服务端就绪冒烟通过（隔离测试库 `127.0.0.1:8098`，`/admin/login` 与 `/front/login` HTTP 200，服务已停止仅余 TIME_WAIT）；代理环境浏览器自动化仍被安全策略拒绝，四视口状态保持 `BLOCKED_BY_BROWSER_POLICY`。新增用户侧脚本 `scripts/run-four-viewport-audit.ps1`：自动准备隔离测试库、启动 8098 服务、以 Edge/Chrome 无头模式对四家族登录页执行四视口截图并归档到 `docs/audits/four-viewport-<时间>/`；脚本头部明示其结果仅为公开页面视觉冒烟，不得表述为完整 Task 10 验收。
- **UI 契约修复**（FrontUiRegressionTest 全量暴露的 3 处历史欠账）：
  - 搜索表单占位符契约：修复 gifts 日期输入缺 placeholder、news 搜索表单残留 `layui-form-label`、risk 十个筛选标签改为 `crm-sr-only`、whs-exp-zero 记录日期输入补 placeholder，共清零 7 处违规；
  - 语言包：`zh-CN`/`en` 的 `common.php` 补齐 `start_date`/`end_date` 两个首屏 key；
  - 主题作用域契约：`front/crmui/layouts/auth.blade.php` 与 `front/crmui/big-agent/layout.blade.php` 由 crmui 与 naive 家族共享（`front-naive/*` 路由），测试契约更新为要求动态 `data-ui-family="{{ $renderFamily }}"`，与运行时主题切换行为一致。
- 验证全部 exit 0：FrontUiRegressionTest `142 tests / 3020 assertions`、news 关键词 `171 tests / 1228 assertions`、risk `207/1376`、whs `49/330`、gift `102/938`、UnifiedBladeDesignSystem `6/65`、VisualCFoundation `18/184`、GlobalCrmThemeCoverage `25/3468`、LegacyUiReplacementCoverage `64/455`、矩阵门禁 `50/3420`；Blade cache/clear 通过。

### 2.20 Phase 7 迁移演练与层级权威重建执行记录（2026-08-22 00:40，经用户明确授权）

- **前置备份**：新增 `scripts/php-db-backup.php`（PDO 只读导出，库名白名单），正式库全量备份至 `storage/app/backups/co_crmv5-backup-20260821-before-phase7.sql`（71 表 / 616 行 / 0.2MB，尾行 `SET FOREIGN_KEY_CHECKS=1;` 完整收尾）。
- **迁移演练**：新增 `scripts/php-sql-execute.php`（mysqli multi_query，仅白名单允许 `full_reset_and_migrate.sql`）；执行约 371 条语句完成「空库重放 + 旧库数据迁移 + 权限表保护 + 密码重置」。迁移后 co_crmv5 共 77 表；关键行数对齐：`user_infos` 17952、`user_logins` 17952、`mt4_users` 17948、`mt4_trades` **872140=旧库精确一致**、`cancel_applies` 29=29；管理员 15 个账号 `abc123` 登录校验 PASS（superadmin/admin/hank 抽验）；`permissions` 238、`roles` 5 未被清空；`migrations` 131 条恢复。
- **层级权威重建**：迁移后只读审计发现 1 条额外闭包关系（16425 活动用户，预期 17669 / 实际 17670）；执行 `agent-hierarchy:rebuild --apply`（自动创建可恢复备份表 `agent_descendants_backup_20260822_001643_aa4c07f0` 与 `user_family_tree_backup_20260822_001643_aa4c07f0`），重建后审计 **17669/17669，缺失/属性错/额外/家谱不一致/软删除残留全部为 0**。
- **迁移后冒烟**：`route:list` 正常输出 1894 行路由；六组测试冒烟全部 exit 0——矩阵门禁 `50/3420`、FrontUiRegression `142/3020`、cancel `75/565`、withdraw `626/6017`、语义门禁 `14/167`、页面渲染门禁 `59/135`，合计 **966 tests / 13324 assertions 全绿**。
- **四视口冒烟执行**：用户侧运行 `scripts/run-four-viewport-audit.ps1` 成功产出 **16/16 张截图**（4 家族登录页 × 1440x900/1280x720/768x1024/390x844）至 `docs/audits/four-viewport-20260822-002017/`。执行中发现并修复脚本三处缺陷：① 文件改为 UTF-8 with BOM 兼容 PowerShell 5.1 中文解析；② 无头浏览器 stderr 噪音（Edge LoadEnclaveImageW 577）不再触发 NativeCommandError 中止；③ finally 改用 `taskkill /T /F` 清理整棵进程树，杜绝孤儿 worker 占用端口。端口复核仅剩正常 TIME_WAIT。
- **边界声明**：16 张截图仅为公开登录页视觉冒烟证据；Task 10 的登录态页面、控制台零错误、交互（菜单/焦点/Escape）、触控 ≥44px 等项仍需按清单人工或授权 Playwright 执行，不得表述为完整四视口验收通过。

### 2.21 全量串行 27+1 条失败根因修复与全绿闭环（2026-08-29 14:00）

- **修复前**：08-28 交接报告遗留全量串行 `4302 tests / 1 error / 27 failures (exit 2)`；本轮两次复现串行（12:26、12:48）确认失败画像稳定：agents_save 4005、FrontAuth step4-9 注册 5000（`代理未配置有效等级`）、FrontBusiness 21 条级联（token NULL）、AdminLegacyAgentStatisticsAdapterTest 指纹 error。
- **根因（MySQL general_log 全程取证，逐条 SQL 铁证）**：`2026_08_01_000002_ensure_front_inviter_test_agent` 迁移在 `migrate:fresh` 的 migrate 阶段创建固定邀请代理 user 10 时，`agent_levels` 尚未被 seeder 填充（空表），回退链落到 `level_id=0`；而历史上由 `FrontDemoDataSeeder` 事后修复该等级的链路自 2026-08-17 22:18（§2.3 Demo Seeder 安全门禁）起在串行中被跳过 → user 10 全程 level_id=0 → 根代理链（`LEGACY_ROOT_AGENT_ID=10`）关系码解析失败 → 27 条失败。「污染事后消失」假象由失败窗口之后的 admin legacy 资料类测试顺带回写 user 10 造成。08-17 11:53 基线全绿是因门禁当时尚未上线。
- **修复**：迁移自举——`agent_levels` 无 `level_code=1` 行时幂等 `insertGetId` 创建基础行（与 InitialDataSeeder 基线一致），user 10 的 level_id 不再可能为 0；生产库该迁移已在册不会重跑。新增回归锁定测试 `FrontInviterAgentLevelBootstrapClosureModuleTest`（2 tests / 6 assertions）。
- **附带修复**：`MySqlTableFingerprint` 指纹不再纳入 `auto_increment`（MySQL 8.0.12 对 I_S.TABLES 自增字段的间歇性陈旧视图导致 `table_fingerprint_mismatch` 间歇 error；行数/内容摘要/结构哈希完全一致仅该字段漂移，自增门禁由 `MySqlAutoIncrementSnapshot::restore()` 独立承担）；`PaymentFixtureContractTest` 契约同步为四字段并新增反向断言（详见 `docs/audits/2026-08-29-fingerprint-contract-sync.md`）。
- **验证**：红绿循环完整（fresh 后 user10 level_id 0→1）；三个原失败类合并回归 `44 tests / 137 assertions` exit 0；最终全量串行 `full-serial-20260829-132018.out`：**`OK (4304 tests, 80304 assertions)` / exit 0；加固后确认串行 `full-serial-20260829-135656.out`：`OK (4305 tests, 80315 assertions)` / exit 0。取证与机制分析详见 `docs/audits/2026-08-29-front-serial-pollution-fix.md`。
- **同轮排查**：全库「译文当提交值」静态扫描 0 命中（`docs/audits/2026-08-29-translation-as-option-value-sweep.md`）；子智能体并行审计确认 Laravel 8.83「事务中途 disconnect 击穿」机制真实存在（`WithdrawalOrderService:298` / `MySqlFixtureMutex:116` 兜底路径），本轮失败非该机制所致，三项防护建议留作下一轮加固。
- **第二轮加固（2026-08-29 下午）**：落实事务击穿护栏——`MySqlFixtureMutex::releaseWithDisconnectFallback` 与 `WithdrawalOrderService::releaseReservationLock` 的 disconnect 兜底均增加 `transactionLevel() > 0` 校验，挂起事务存在时跳过断开并失败关闭（不再静默击穿测试事务）；互斥锁单测扩为 3 tests（含跳过路径），提现全量回归 `635 tests / 6075 assertions` exit 0。`AgentLevelController@destroy` 的无引用校验为文档明示契约，维持不变。
- **第三轮：后台批量导入 CSV 上传 UI 闭环（2026-08-29 下午）**：补齐 08-28 交接报告 §五.1 缺口——授信/入金/出金三个导入模块在 Layui 与 CrmUI 双家族新增「导入 CSV 批量数据」入口（共享 CrmUpload deferred 模式 + CrmAjax.upload 携带管理员 JWT；后端整批校验、单事务落库契约不变）；语言键 admin.import_csv_* 与 crmui.actions.import 双语齐备；UI 契约断言扩展（15 tests / 170 assertions）、组合回归 175 tests 全绿、最终串行 full-serial-20260829-144445.out：OK (4305 tests, 80343 assertions) / exit 0。详见 docs/audits/2026-08-29-admin-batch-import-upload-ui.md。
- **第四轮：旧前台资料页上传组件并入共享 layui-upload（2026-08-29 傍晚）**：关闭 08-28 交接报告 §五.2 刻意保留项——legacy-action 页四类文件字段（Idphoto1/Idphoto2/bankimg/headimg）全部迁移到共享 CrmUpload 组件（deferred 模式），旧 Session/CSRF/err-col 错误位/验证码倒计时契约原样保全，提交按旧字段名追加组件缓存 File；页面契约测试按同等意图演进（3 tests / 105 assertions），上传与资料相关合并回归 217 tests / 7128 assertions 全绿，最终串行 full-serial-20260829-151746.out：OK (4305 tests, 80348 assertions) / exit 0。详见 docs/audits/2026-08-29-front-legacy-profile-upload-migration.md。
- **第六轮：全模块对照审计 + 中文注释标准 v0.0.3 落地（2026-08-29 晚）**：①独立对照审计（scripts/audit-module-parity.php + classify-unmapped-methods.php + audit-old-routes-coverage.php）确认旧项目 439 个控制器公共方法中全部活动路由方法均已在矩阵闭环，66 个未路由方法定性为旧项目死代码/基类内部（含 resource CRUD 残留、支付 SDK 内部访问器）；发现并补记唯一矩阵缺行 POST test/withdraw（475→476，四处账本同步、15 个门禁基线更新，MatrixClosure 50 tests 全绿）；前端 223 个旧视图由四套 UI 家族承接并以 LegacyUiReplacementCoverageTest 等门禁锁定。②注释标准 v0.0.3 工具链落地：tools/add_file_headers.php（1483 文件补齐 PhpStorm 头部）+ tools/audit-members.php + composer 脚本 + tests/Unit/MemberCommentCoverageTest CI 门禁；881 处成员中文注释经双子智能体并行补齐（118+128 文件），全库审计缺失 0。③最终全量串行 full-serial-20260829-182233.out：OK (4306 tests, 80366 assertions) / exit 0。详见 docs/audits/2026-08-29-module-parity-and-comment-standard-audit.md。
- **第七轮：注释标准深补 + 注释风暴零破坏自检（2026-08-29 深夜）**：§4.2 文件级功能说明 577 文件补齐（A：tests/routes 335；B：app/database/config 242）、§6.1 配置键中文说明 595 键补齐（crm_themes 302/logging 114/database 42/app 39/trace 20 等，有序键值对比对 0 差异）；四项机器审计终值全部清零（头部 1483/1483、文件级说明 1160/1160、成员注释 1203/1203、配置键 912/912）。注释风暴后自检：四家族契约门禁 268 tests 全绿、逐文件隔离复跑 679/679 全绿、最终串行 OK (4306 tests, 80366 assertions) / exit 0。
- **第八轮：user_infos.comm_rate 百分数语义统一（2026-08-29 深夜，依据用户『每一逻辑百分百正确』指令修复 08-28 交接报告 §四.1）**：证据链确认全代码库唯一语义为百分数 0..100（整数列、种子/生产 65/85、佣金引擎 /100、注册阈值 <=50、建档 min(max_commission=85)、旧后台 max:100），唯一缺陷离群点为 AgentController::updateCommission 的 max:1（另一处位于未路由死控制器 UserController，已同步修正）；layui 佣金表单 max=1/step=0.0001 同步改 max=100/step=1 并修正误导注释；夹具 0.2→20；新增语义锁定测试 AdminCommRatePercentSemanticsClosureModuleTest（85 原样落库/150 越界拒绝/澄清 updateUser 白名单不含 comm_rate）。佣金/代理/注册族回归 322 tests 全绿，最终串行 full-serial-20260829-204006.out：OK (4308 tests, 80371 assertions) / exit 0。详见 docs/audits/2026-08-29-comm-rate-percent-semantics-fix.md。
- **第九轮：浏览器策略阻塞解除与登录态四视口首轮验收（2026-08-29 深夜）**：通过 ZCode 真实浏览器控制通道（此前被策略阻塞的无头 CLI 之外的可用通道）完成 Phase 2 Task 10 首轮登录态四视口验收：四家族登录页 4 视口全量 + 登录态仪表盘与代表模块页抽样，28 张截图归档 `docs/audits/four-viewport-20260829-browser/`；确认登录直达 Dashboard（诉求 1）、图标化语言/主题、实名引导按钮、8 tab 流水 + 靠左统计 div 真实渲染；观察项 1 条（admin-crmui 登录页 Loading 残留文案）。详见 docs/audits/2026-08-29-browser-four-viewport-login-state-validation.md。
- **待人工决策项仅剩一项**：~~`user_infos.comm_rate` 三方契约~~（✅ 已于 2026-08-29 按百分数 0..100 统一修复，见第八轮）；`2026_08_28_000001_add_mt4_trades_rebate_lookup_index` 尚未在正式库执行（ALTER 约 24 秒，需排期；控制器降级路径已核验，生产当前正确运行）。

### 3. 双 UI 家族页面族

- `resources/admin/{layui,crmui}` 各约 49 个目录；`resources/front/{layui,crmui}` 各约 19 个目录。
- 视觉 C 最新专项：`GlobalCrmThemeCoverageTest`（25 tests / 3468 assertions）、`UnifiedBladeDesignSystemTest`（6 tests / 65 assertions）、`VisualCFoundationContractTest`（18 tests / 184 assertions）、`LegacyUiReplacementCoverageTest`（64 tests / 455 assertions）。
- 本轮 `FrontUiRegressionTest --filter crmui` 为 `30 tests / 721 assertions`，`--filter trade_symbol` 为 `2 tests / 62 assertions`；2026-08-18 最新完整回归为 `141 tests / 2985 assertions`，后台出金搜索 placeholder、中文 `user.updatedAt`、平面主题 token 与周期多语言门禁均已通过。

### 4. 代理层级闭包权威化（2026-08-16 完成）

- `user_infos.parent_id` 为唯一拓扑事实源；`family_tree`、`agent_descendants` 均由 parent_id 链派生。
- `app/Services/FamilyTreeService.php` 已实现：`getAncestors`、`getAllDescendants`、`rebuildFamilyTree`、`rebuildDescendants`、`rebuildAllHierarchy`、`auditHierarchy` 等全套方法。
- 新命令 `php artisan agent-hierarchy:rebuild`：默认只读审计，`--apply` 才备份后事务化重建（含失败回滚）。
- 审计脚本 `scripts/audit_agent_descendants.php`：含 128 级深度、孤儿/循环/非代理父级/软删除残留分类。
- 专项测试：`AgentHierarchyClosureRebuildTest`（34KB）+ `AgentHierarchyMigrationContractTest` 已就位。

### 5. 全量迁移 SQL 已构建（2026-08-17）

- `scripts/build-full-migration-sql.php` → `database/sql/full_reset_and_migrate.sql`
- 单文件可执行：最新 schema 空库重放 + 旧库全量数据迁移 + 权限体系初始化表保护（permissions/menus/role_permissions 等）+ 密码本地化重置 abc123。
- ⚠️ 执行会清空 `co_crmv5` 全部业务表，属不可逆操作，**必须备份后且经用户再次明确授权才可执行**。

---

## 四、即将需要做的功能逻辑（详细清单）

### A. Phase 2 收尾（当前阻塞项）

1. **Task 10 浏览器验收矩阵**：每个 canonical 页面需在 `1440x900 / 1280x720 / 768x1024 / 390x844` 四视口验证：
   - HTTP 正常、控制台 0 application error；
   - 无横向溢出、无不合理重叠、文字与操作不被遮挡；
   - 可见键盘焦点、触控目标 ≥44px；
   - 表格/表单 loading / empty / error / disabled 状态可辨识；
   - 两套壳移动端侧栏打开/关闭/遮罩/Escape/焦点恢复；
   - 表单客户端无效输入校验，不提交会写库的有效业务请求；
   - 截图写入新 Phase 2 审计目录并校验 PNG。
   - 当前状态（2026-08-29 更新）：**浏览器策略阻塞已解除**——通过 ZCode 真实浏览器控制通道（非无头 CLI）完成首轮登录态四视口验收：四家族登录页 4 视口全量（16 张）+ 登录态仪表盘（layui 双家族 4 视口、crmui 双家族桌面抽样）+ 两个代表模块页，合计 **28 张截图**归档于 `docs/audits/four-viewport-20260829-browser/`，详见 `docs/audits/2026-08-29-browser-four-viewport-login-state-validation.md`。登录直达 Dashboard、语言/主题图标化、实名引导按钮、8 tab 流水 + 靠左统计 div 等真实浏览器渲染全部确认。完整模块矩阵（~50 模块 × 4 视口）与交互/焦点/触控走查可沿同一通道分批执行。
2. 浏览器通过后：重新启动测试库隔离服务，完成截图并将 Task 10/11 证据回写 phase-2 报告；不得将当前静态检查表述为浏览器通过。

### B. 剩余路由业务核验 —— 已全部完成（2026-08-21）

原 41 条待办已全部闭环。已闭环批次：`WithdrawAmountController` 19 条、`WithdrawStatusController` 12 条、前台 `BigNumberController` 业务路由 10 条、`FengXianManageController` 9 条、`NewsInfoController` 7 条、`AdminWhsExpZeroController` 6 条、`GiftController` 6 条、`CancellationController` 6 条、后台 `PositionSummaryController` 5 条、`RightsSummaryController` 5 条、`AdminOpenOrder / AdminCloseOrder` 3+3 条、`AdminRealCommissionController` 3 条、`BatchCreditController` 3 条、`AdminProductionController` 3 条、`WithdrawFlowController` 2 条、`DepositAmountController` 2 条、`ExchangeRateController` 2 条、`UserLoginOnlineController` 2 条、`PayChannelController` 1 条、`UnDepositAmountController` 1 条（收官合并批）。**剩余待办表已清空。**

> 收官说明：上表所列全部条目已于 2026-08-21 随 dmin_funding_ops_misc_business_2026_08_21 证据组闭环，业务核验矩阵达到 475/475 verified、0 待人工核验、0 未解决旧源码、0 未映射路由。此后任何新批次不得再引用本表作为待办依据。

### C. 前台双 UI 真实数据源审计（2026-08-17）

- 已建立 `docs/audits/2026-08-17-front-dual-ui-db-data-source-audit.md`，覆盖 dashboard、account、position、orders、flow、commission、agent、deposit、withdraw、gift、news、profile、big-agent 的“页面 → API → Controller → 新库表 → 测试”链路。
- 生产前台未发现空数据 mock fallback；动态品种统一读取 `symbol_prices`，Naive 复用 CrmUI canonical 页面和 `/api/front/*` 数据源。
- 前台大代理列表、持仓汇总、未平仓和已平仓页面/搜索 10 条已写七维证据组，矩阵据此从出金批次后的 `384/91` 继续推进到 `394/81`；`FrontBigNumberBusinessMatrixClosureTest` 已验证方法级映射和证据结构。
- 已关闭风险：默认 `DatabaseSeeder` 现有安全环境 + 显式布尔开关双门禁，不会在 production 调用 `FrontDemoDataSeeder`。
- 已关闭差异：`position/summary2` 使用独立现代本人汇总入口；账户类型切换已覆盖三套视觉家族；Big Agent 已补齐 `/front-naive/big-agent/*` 且保持旧会话/API 边界。
- `front-dual-ui-db-data-source-closure` 计划已完成并回写全部步骤；其结论覆盖真实数据源、动态品种和既有高风险模块运行时证据，不替代仓位总结旧逻辑等价或四视口浏览器验收。

### D. Phase 3–7 规划工作（路线图已定义，待逐阶段实施）

- **Phase 3 账户与交易**：状态机、Decimal 精度、幂等、并发及双 UI 闭环；MT4 真实连接保持禁用。
- **Phase 4 资金业务**：金额守恒、回调幂等、审批并发、失败补偿和双 UI 闭环。
- **Phase 5 返佣与结算**：计算口径、周期、层级归属、重复结算防护和双 UI。
- **Phase 6 运营配置**：配置生效、缓存失效、内容状态、审计和双 UI。
- **Phase 7 隔离迁移与全量验收**：~~schema 空库重放、旧库数据迁移演练~~ ✅ 已于 2026-08-22 经用户授权执行完成（见 §2.20）；剩余：~~完整串行 PHPUnit 复跑、逐文件隔离测试、最终差异报告~~ ✅ 已于 2026-08-29 完成（4305 tests 全绿 ×4 轮；678 个测试文件逐文件独立进程全部通过；见 docs/audits/2026-08-29-phase7-final-difference-report.md）；剩余：登录态全 Blade 浏览器验证（持续 BLOCKED_BY_BROWSER_POLICY）。

### E. 其他待办与建议

1. ~~`php artisan agent-hierarchy:rebuild --apply` 数据修复~~ ✅ **已完成（2026-08-22）**：迁移演练后审计发现 1 条额外关系，`--apply` 权威重建后 17669/17669 全部归零，自动备份表 `agent_descendants_backup_20260822_001643_aa4c07f0` / `user_family_tree_backup_20260822_001643_aa4c07f0` 可恢复。
2. ~~`storage/logs/laravel.log` 已达 2.9GB，建议配置日志轮转或清理。~~ ✅ **已完成（2026-08-21）**：`config/logging.php` 默认 stack 通道由 `single` 改为 `daily`（按天滚动保留 14 天）；原 3344.6MB 的 `laravel.log` 已截断归零，末尾 2000 行存档至 `storage/logs/laravel-archive-20260821-before-rotation.log`（293KB）。
3. ~~根目录 `README.md` 仍是旧 Go WebSocket 项目的说明。~~ ✅ **已完成（2026-08-21）**：已重写为当前 Laravel CRM 项目文档（技术栈、三库写入约束、快速开始、目录结构、旧路由兼容层、测试与运维命令、安全红线、文档索引）。
4. `docs/audits/旧项目模块逻辑迁移核验矩阵.md` 已以 UTF-8 重新生成；后续每次生成仍需保留编码与替换字符静态门禁（本轮历次再生成均已执行 0 替换字符校验）。

---

## 五、总体结论

- 已完成 **100%**（475/475 路由业务核验全部闭环）。出金申请 19 条、出金状态 12 条、前台大代理业务 10 条、FengXian 风控 9 条、News 7 条、WHS 6 条、Gift 6 条、Cancellation 6 条、后台 PositionSummary 5 条、RightsSummary 5 条、OpenCloseOrder 6 条、RealCommission 3 条、BatchCredit 3 条、Production 3 条与资金运营收官 10 条已全部进入权威矩阵；测试基础设施、双 UI 页面族、代理拓扑闭包和迁移 SQL 均已就绪。
- **剩余核心工作**：仅剩 Phase 2 Task 10/11 四视口浏览器视觉验收（持续 `BLOCKED_BY_BROWSER_POLICY`）与 Phase 7 隔离迁移演练（迁移 SQL 已构建，执行需用户再次明确授权）；475 条旧路由的业务逻辑等价替换与双 UI 闭环已全部完成。
- 全程约束：旧库只读、正式新库禁写、任一门禁失败即停止、不伪造提交记录。

---

## 附：关键文件索引

| 文件 | 说明 |
| --- | --- |
| `storage/app/audits/旧项目模块逻辑迁移核验矩阵.json` | 475 条路由核验矩阵（权威进度账本） |
| `docs/audits/2026-08-10-phase-2-identity-and-organization-result.md` | Phase 2 结果审计报告 |
| `docs/superpowers/plans/2026-08-07-full-legacy-parity-and-visual-c-roadmap.md` | Phase 0–7 总路线图 |
| `docs/superpowers/plans/2026-08-16-agent-hierarchy-closure-completion.md` | 代理层级闭包收尾计划（已完成实施） |
| `docs/superpowers/plans/2026-08-16-agent-descendants-authoritative-rebuild.md` | 代理层级闭包权威重建计划（已完成实施） |
| `docs/superpowers/plans/2026-08-17-admin-withdraw-amount-parity.md` | 后台出金 19 条闭环计划（已完成实施与双重复审） |
| `docs/superpowers/plans/2026-08-17-front-dual-ui-db-data-source-closure.md` | 前台双 UI 数据源审计计划（已完成；残余风险拆入后续专项） |
| `docs/superpowers/plans/2026-08-17-dashboard-shell-ux-round.md` | Dashboard Shell UX 计划（代码与自动化完成；四视口被浏览器策略阻塞） |
| `docs/audits/2026-08-17-dashboard-shell-ux-round-result.md` | Dashboard Shell UX 结果审计与浏览器阻塞证据 |
| `docs/superpowers/plans/2026-08-18-admin-risk-fengxian-parity.md` | FengXian 风控 9 条闭环计划（已完成实施、双重复审与矩阵回写；浏览器策略阻塞） |
| `docs/superpowers/plans/2026-08-19-admin-whs-exp-zero-parity.md` | WHS 仓位清零 6 条闭环计划（已完成实施、双重复审与矩阵回写；浏览器策略阻塞） |
| `app/Services/FamilyTreeService.php` | 代理层级派生服务 |
| `app/Console/Commands/RebuildAgentHierarchy.php` | `agent-hierarchy:rebuild` 命令 |
| `scripts/audit_agent_descendants.php` | 闭包只读审计脚本 |
| `database/sql/full_reset_and_migrate.sql` | 全量重置+迁移候选 SQL（待授权执行） |
| `storage/logs/full-serial-20260817-113701.out` | 全量测试日志（3641 tests 通过） |
