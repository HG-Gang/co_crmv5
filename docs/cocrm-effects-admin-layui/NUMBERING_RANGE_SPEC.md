# CoCRM v5 效果图编号区间规范

> 本文档为效果图反馈的唯一编号规范。所有编号全局唯一、从 `#0001` 开始严格递增，且文件名、图片左上角标签、`PAGE_EFFECT_INDEX.csv` 与 `PAGE_EFFECT_INDEX.md` 必须保持一致。

## 编号区间总览

| 编号范围 | 界面端 / UI 家族 | 图片目录 | 数量 | 说明 |
|---|---|---|---:|---|
| `#0001–#0032` | **前台 Layui** | `front/layui/` | 32 | 前台用户门户的 Layui 经典界面族 |
| `#0033–#0054` | **前台 CrmUI** | `front/crmui/` | 22 | 前台 CrmUI 新界面族；包含 `big-agent/` 大代理独立入口 |
| `#0055–#0099` | **后台 Layui** | `admin/layui/` | 45 | 后台运营管理的 Layui 经典界面族 |
| `#0100–#0136` | **后台 CrmUI** | `admin/crmui/` | 37 | 后台运营管理的 CrmUI 新界面族 |

## 大代理入口说明

大代理独立入口位于前台 CrmUI 目录 `front/crmui/big-agent/`，其子区间为 `#0048–#0054`。它属于前台 CrmUI 的连续大区间，不另起编号。

## 编号使用规则

当需要修改任意页面时，只需引用图片编号。例如，“调整 **#0070** 的出金审核筛选区”即可定位到对应图片、路由和视图。不得因文件移动、重新打包或目录调整而复用、删除或重排既有编号；如新增页面，必须从当前最大编号之后继续递增。

## 校验结果

本次共生成 **136** 张页面效果图，已验证序号为 `#0001–#0136`，无重复、无缺号，并且四套 UI 家族的编号区间互不交叠。
