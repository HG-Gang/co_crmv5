# 2026-08-01 后台遗留路由闭环与全量回归报告

> 切片：后台 7 条遗留路由缺口全部闭环保绿 + 前台 3 项缺口回归验证；全量回归清零。

## 一、本轮目标

以旧项目 `D:\Php-project\Php\new_co_gmtk_crmV3` 为源，新项目 `D:\Software\PhpProject\Demo\co_crmv5` 为目标，将遗留路由核验矩阵中剩余的后台 7 条路由缺口逐条补齐（真实验证 → 修复 → 闭环保绿），并完成整轮回归清零与 flake 判定。

## 二、后台 7 条遗留路由闭环明细

| 旧路由 | 语义 | 现代映射 | 落地位置 | 测试 |
|---|---|---|---|---|
| `index/admin/order/v2/parentPath` | 向上链路（上级代理链） | `admin_api_agentParentPath`（新增 POST `/agentParentPath`） | `routes/admin.php`、`AgentController@parentPath`、`LegacyAdminController::targetRouteFor` | `AdminLegacyOrderParentPathClosureModuleTest`（6 tests） |
| `index/admin/cust_apply_pass` | 账户类型变更审批通过 | `admin_api_confirmAgent` | `targetRouteFor` + `forwardLegacyCustApply` | `AdminLegacyCustApplyApprovalClosureModuleTest`（5 tests） |
| `index/admin/cust_apply_nopass` | 账户类型变更审批驳回 | `admin_api_rejectAgentConfirmation` | 同上 | 同上 |
| `index/admin/auth/user_idcard_bank` | 实名认证两证审核 | `admin_api_reviewAuth` | `forwardLegacyUserIdCardBank`（reason 取 idcard_reason 优先，两证 `'0'`=status 1 否则 2） | `AdminLegacyUserIdCardBankClosureModuleTest`（5 tests） |
| `index/admin/risk/IpaddressDeatail/{idaddr}` | 风险 IP 详情 | `admin_api_riskIpDetail`（下划线还原 + IP 校验） | `forwardLegacyIpAddressDetail` | `AdminLegacyIpAddressDetailClosureModuleTest`（3 tests） |
| `index/admin/agents/agents_edit_info/{uid}` | 代理编辑页透传 uid | 渲染 `admin_layui::agents.index` + `data-legacy-uid` | `handle` 兜底 + `resources/admin/layui/agents/index.blade.php` | `AdminLegacyAgentEditInfoClosureModuleTest`（2 tests） |
| `index/admin/agents/oneKeySearch` | 列表即扫描（白名单排除 + userId 别名） | `admin_api_whsExpZeroList` | `targetRouteFor` | `AdminLegacyOneKeySearchClosureModuleTest`（3 tests） |

补充闭环（依赖项）：
- `agents_save` / `cust_save_add` / `cust_save_info`：既有 `AdminLegacyAgentUserSaveClosureModuleTest`（5 tests）覆盖，含真实 provisioning 处理链（`UserRegistrationService → UserMt4ProvisioningProcessor → Mt4UserProvisioningGateway`，匿名 stub 注入 + customer 序列 fixture 987900）。
- 权限行：新增 `admin_agent_parent_path`（`2026_06_06_000004` 补 action + `2026_07_19_000001` 补兜底行，dev 库已落库 id=17077，parent=admin_agents）。

## 三、前台 3 项缺口回归

`FrontLegacyCheckEmailClosureModuleTest`、`FrontLegacyLogoutClosureModuleTest`、`FrontLegacyDashboardWiringClosureModuleTest` 均保持绿色（本片全量回归内验证）。

## 四、权限与文档联动修复

新增权限后触发的关联测试修复：
- `AdminProtectedRoutePermissionClosureModuleTest`：迁移补行 `admin_agent_parent_path` → 4 tests / 420 assertions 绿。
- `AdminPermissionNameReferenceDocumentationTest` / `AdminPermissionNameReferenceRowCompletenessTest`：`docs/admin-permission-name-reference.md` 补行（id 17077）+ 总数 270→271、启用 250→251 → 绿（2 tests / 3562 assertions）。

## 五、全量回归数据

| 轮次 | 结果 | 说明 |
|---|---|---|
| fullrun2 | 10 failed | 本片起点 |
| fullrun3 | 1 failed | `AdminProtectedRoutePermissionClosureModuleTest:71` 缺 `admin_api_agentParentPath` 权限行 |
| fullrun4 | 2 failed | 权限行修复后触发的文档联动缺失（总数/行数 270 vs 271） |
| **fullrun5** | **2894 passed / 0 failed** | **清零**（1285.65s） |

- `CrmUiStackTest`（此前 1/3 flake 概率）：fullrun4、fullrun5 均通过，独立复跑 4 tests / 443 assertions 绿 —— 判定：未再现 flake，通过。
- 批次定向复跑：8 个 AdminLegacy* 套件 + 权限/文档套件全部绿（46 tests 合并批次 + 关联套件）。

## 六、结论

- 后台遗留路由矩阵 7 条缺口全部闭环（新增 29 个测试用例、226+ assertions）。
- 全量 2894 tests 全绿，无 flake 残留；权限、文档、迁移三方一致。
- 待后续切片：矩阵其余未核验模块（如需）与前台深水区人工回归。

## 相关文件

- 修复/新增：`app/Http/Controllers/Admin/LegacyAdminController.php`（handle、targetRouteFor、forwardLegacyCustApply、forwardLegacyUserIdCardBank、forwardLegacyIpAddressDetail）、`app/Http/Controllers/Admin/AgentController.php`（parentPath 系列）、`routes/admin.php`、`resources/admin/layui/agents/index.blade.php`、`database/migrations/2026_06_06_000004_add_admin_business_module_permissions.php`、`database/migrations/2026_07_19_000001_ensure_protected_admin_route_permissions.php`、`docs/admin-permission-name-reference.md`
- 新增测试：`tests/Feature/AdminLegacy{OrderParentPath,CustApplyApproval,UserIdCardBank,IpAddressDetail,AgentEditInfo,AgentUserSave,OneKeySearch}ClosureModuleTest.php`
- 参考矩阵：`docs/audits/旧项目模块逻辑迁移核验矩阵.md`
